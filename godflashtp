-- =============================================
-- LAW HUB - FUN HUB STYLE
-- Instant Steal | Active Potion | Unlock Base | Actions
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer

-- COLORS
local Colors = {
    Primary = Color3.fromRGB(255, 215, 0),
    BG = Color3.fromRGB(20, 20, 20),
    Panel = Color3.fromRGB(30, 30, 30),
    Text = Color3.fromRGB(255, 255, 255),
    ToggleOn = Color3.fromRGB(255, 215, 0),
    ToggleOff = Color3.fromRGB(50, 50, 50),
}

-- TOGGLES
local Toggles = {
    steal = false,
    potion = false,
    unlock = false,
    actions = false,
}

-- VARIABLES
local RootPart = nil
local Humanoid = nil
local StealLoop = nil
local PotionConnection = nil
local UIPanels = {}

-- =============================================
-- UTILITIES
-- =============================================
local function Tween(obj, props, time)
    TweenService:Create(obj, TweenInfo.new(time or 0.2), props):Play()
end

local function MakeCorner(obj, r)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, r or 8)
    c.Parent = obj
end

local function MakeStroke(obj, color, thick)
    local s = Instance.new("UIStroke")
    s.Color = color or Colors.Primary
    s.Thickness = thick or 1.5
    s.Parent = obj
end

-- =============================================
-- CHARACTER SETUP
-- =============================================
LocalPlayer.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    RootPart = char:FindFirstChild("HumanoidRootPart")
    Humanoid = char:FindFirstChildOfClass("Humanoid")
end)

local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
RootPart = char:FindFirstChild("HumanoidRootPart")
Humanoid = char:FindFirstChildOfClass("Humanoid")

-- =============================================
-- 1. INSTANT STEAL
-- =============================================
local function FindStealPrompt()
    local plots = workspace:FindFirstChild("Plots")
    if not plots or not RootPart then return nil end
    local closest = nil
    local minDist = math.huge
    for _, plot in pairs(plots:GetChildren()) do
        for _, obj in pairs(plot:GetDescendants()) do
            if obj:IsA("ProximityPrompt") and obj.ActionText == "Steal" and obj.Enabled then
                local part = obj.Parent
                local pos = nil
                if part:IsA("BasePart") then
                    pos = part.Position
                elseif part:IsA("Model") and part.PrimaryPart then
                    pos = part.PrimaryPart.Position
                end
                if pos then
                    local d = (RootPart.Position - pos).Magnitude
                    if d < minDist then
                        minDist = d
                        closest = obj
                    end
                end
            end
        end
    end
    return closest
end

local function FirePrompt(prompt)
    if not prompt then return end
    pcall(function()
        fireproximityprompt(prompt, 9999)
        prompt:InputHoldBegin()
        task.wait(0.05)
        prompt:InputHoldEnd()
    end)
end

local function GetMyPlot()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    for _, plot in pairs(plots:GetChildren()) do
        local sign = plot:FindFirstChild("PlotSign")
        if sign and sign:FindFirstChild("YourBase") and sign.YourBase.Enabled then
            return plot
        end
    end
    return nil
end

local function TeleportTo(pos)
    if RootPart then
        RootPart.CFrame = CFrame.new(pos)
        RootPart.AssemblyLinearVelocity = Vector3.zero
    end
end

local function EquipCarpet()
    local char = LocalPlayer.Character
    local bp = LocalPlayer:FindFirstChild("Backpack")
    if not char or not bp then return end
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") then
            tool.Parent = bp
        end
    end
    local carpet = bp:FindFirstChild("Flying Carpet")
    if carpet and Humanoid then
        Humanoid:EquipTool(carpet)
    end
end

local function DoSteal()
    local plot = GetMyPlot()
    if not plot then return end
    EquipCarpet()
    task.wait(0.1)
    local order = plot:GetAttribute("Order")
    if order == 2 then
        TeleportTo(Vector3.new(-368, -7, 69))
        task.wait(0.1)
        TeleportTo(Vector3.new(-336, -5, 100))
        task.wait(0.1)
        TeleportTo(Vector3.new(-352, -7, 76))
    elseif order == 1 then
        TeleportTo(Vector3.new(-368, -7, 42))
        task.wait(0.1)
        TeleportTo(Vector3.new(-336, -5, 20))
        task.wait(0.1)
        TeleportTo(Vector3.new(-353, -7, 44))
    end
    FirePrompt(FindStealPrompt())
end

local function StartStealLoop()
    if StealLoop then StealLoop:Disconnect() end
    StealLoop = RunService.Heartbeat:Connect(function()
        if Toggles.steal and Humanoid and Humanoid.Health > 0 then
            DoSteal()
            task.wait(1.5)
        end
    end)
end

local function StopStealLoop()
    if StealLoop then
        StealLoop:Disconnect()
        StealLoop = nil
    end
end

-- =============================================
-- 2. ACTIVE POTION
-- =============================================
local function UseGiantPotion()
    local bp = LocalPlayer:FindFirstChild("Backpack")
    local char = LocalPlayer.Character
    if not bp or not char then return end
    local potion = bp:FindFirstChild("Giant Potion")
    if potion then
        potion.Parent = char
        pcall(function() potion:Activate() end)
    end
end

local function StartPotionWatcher()
    if PotionConnection then PotionConnection:Disconnect() end
    local gui = LocalPlayer:WaitForChild("PlayerGui")
    
    local function CheckText(text)
        if Toggles.potion and text and string.find(string.lower(text), "steal") then
            UseGiantPotion()
        end
    end
    
    for _, obj in pairs(gui:GetDescendants()) do
        if obj:IsA("TextLabel") then
            CheckText(obj.Text)
            obj:GetPropertyChangedSignal("Text"):Connect(function() CheckText(obj.Text) end)
        end
    end
    
    PotionConnection = gui.DescendantAdded:Connect(function(d)
        if d:IsA("TextLabel") then
            CheckText(d.Text)
            d:GetPropertyChangedSignal("Text"):Connect(function() CheckText(d.Text) end)
        end
    end)
end

local function StopPotionWatcher()
    if PotionConnection then
        PotionConnection:Disconnect()
        PotionConnection = nil
    end
end

-- =============================================
-- 3. UNLOCK BASE (OTHER PLAYERS)
-- =============================================
local function FindOtherBase()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    for _, plot in pairs(plots:GetChildren()) do
        local sign = plot:FindFirstChild("PlotSign")
        if sign then
            local yourBase = sign:FindFirstChild("YourBase")
            if not (yourBase and yourBase.Enabled) then
                return plot
            end
        end
    end
    return nil
end

local function DoUnlock()
    local plot = FindOtherBase()
    if not plot then return end
    for _, obj in pairs(plot:GetDescendants()) do
        if obj:IsA("ProximityPrompt") and obj.Enabled then
            local txt = (obj.ActionText or ""):lower()
            if txt:find("unlock") then
                local oldDist = obj.MaxActivationDistance
                obj.MaxActivationDistance = 9999
                pcall(function() fireproximityprompt(obj) end)
                task.delay(0.3, function() obj.MaxActivationDistance = oldDist end)
                break
            end
        end
    end
end

-- =============================================
-- 4. ACTIONS
-- =============================================
local function Rejoin()
    TeleportService:Teleport(game.PlaceId, LocalPlayer)
end

local function KickSelf()
    LocalPlayer:Kick("Kicked by Law Hub")
end

local function RagdollSelf()
    if Humanoid then
        Humanoid:ChangeState(Enum.HumanoidStateType.Physics)
    end
end

local function ResetChar()
    if LocalPlayer.Character then
        LocalPlayer.Character:Destroy()
    end
end

-- =============================================
-- CREATE TOGGLE SWITCH (LIKE FUN HUB)
-- =============================================
local function CreateToggle(parent, y, label, desc, id, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -20, 0, 52)
    frame.Position = UDim2.new(0, 10, 0, y)
    frame.BackgroundColor3 = Colors.Panel
    frame.BackgroundTransparency = 0.3
    frame.BorderSizePixel = 0
    frame.Parent = parent
    MakeCorner(frame, 8)
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -70, 0, 22)
    title.Position = UDim2.new(0, 12, 0, 4)
    title.BackgroundTransparency = 1
    title.Text = label
    title.Font = Enum.Font.GothamBold
    title.TextSize = 13
    title.TextColor3 = Colors.Text
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = frame
    
    local descLabel = Instance.new("TextLabel")
    descLabel.Size = UDim2.new(1, -70, 0, 18)
    descLabel.Position = UDim2.new(0, 12, 0, 28)
    descLabel.BackgroundTransparency = 1
    descLabel.Text = desc
    descLabel.Font = Enum.Font.Gotham
    descLabel.TextSize = 10
    descLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
    descLabel.TextXAlignment = Enum.TextXAlignment.Left
    descLabel.Parent = frame
    
    -- Switch BG
    local switchBg = Instance.new("Frame")
    switchBg.Size = UDim2.new(0, 44, 0, 24)
    switchBg.Position = UDim2.new(1, -56, 0.5, -12)
    switchBg.BackgroundColor3 = Toggles[id] and Colors.ToggleOn or Colors.ToggleOff
    switchBg.BorderSizePixel = 0
    switchBg.Parent = frame
    MakeCorner(switchBg, 12)
    
    -- Knob
    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 20, 0, 20)
    knob.Position = Toggles[id] and UDim2.new(1, -22, 0.5, -10) or UDim2.new(0, 2, 0.5, -10)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.BorderSizePixel = 0
    knob.Parent = switchBg
    MakeCorner(knob, 10)
    
    -- Click button
    local click = Instance.new("TextButton")
    click.Size = UDim2.new(1, 0, 1, 0)
    click.BackgroundTransparency = 1
    click.Text = ""
    click.Parent = switchBg
    
    click.MouseButton1Click:Connect(function()
        Toggles[id] = not Toggles[id]
        
        if Toggles[id] then
            Tween(switchBg, {BackgroundColor3 = Colors.ToggleOn}, 0.15)
            Tween(knob, {Position = UDim2.new(1, -22, 0.5, -10)}, 0.15)
        else
            Tween(switchBg, {BackgroundColor3 = Colors.ToggleOff}, 0.15)
            Tween(knob, {Position = UDim2.new(0, 2, 0.5, -10)}, 0.15)
        end
        
        callback(Toggles[id])
    end)
    
    -- Hover effect
    frame.MouseEnter:Connect(function()
        Tween(frame, {BackgroundTransparency = 0}, 0.1)
    end)
    frame.MouseLeave:Connect(function()
        Tween(frame, {BackgroundTransparency = 0.3}, 0.1)
    end)
    
    return frame
end

-- =============================================
-- CREATE UI PANELS (APPEAR WHEN TOGGLE ON)
-- =============================================
local function ShowStealPanel(show)
    if show then
        if UIPanels.steal then UIPanels.steal:Destroy() end
        local sg = Instance.new("ScreenGui")
        sg.Name = "LawStealPanel"
        sg.ResetOnSpawn = false
        sg.Parent = LocalPlayer.PlayerGui
        
        local f = Instance.new("Frame")
        f.Size = UDim2.new(0, 180, 0, 40)
        f.Position = UDim2.new(0.5, -90, 0.82, 0)
        f.BackgroundColor3 = Colors.BG
        f.BackgroundTransparency = 0.1
        f.BorderSizePixel = 0
        f.Parent = sg
        MakeCorner(f, 10)
        MakeStroke(f, Colors.Primary, 1.5)
        
        local txt = Instance.new("TextLabel")
        txt.Size = UDim2.new(1, 0, 1, 0)
        txt.BackgroundTransparency = 1
        txt.Text = "⚡ INSTANT STEAL"
        txt.Font = Enum.Font.GothamBold
        txt.TextSize = 12
        txt.TextColor3 = Colors.Primary
        txt.Parent = f
        
        UIPanels.steal = sg
    else
        if UIPanels.steal then UIPanels.steal:Destroy(); UIPanels.steal = nil end
    end
end

local function ShowPotionPanel(show)
    if show then
        if UIPanels.potion then UIPanels.potion:Destroy() end
        local sg = Instance.new("ScreenGui")
        sg.Name = "LawPotionPanel"
        sg.ResetOnSpawn = false
        sg.Parent = LocalPlayer.PlayerGui
        
        local f = Instance.new("Frame")
        f.Size = UDim2.new(0, 180, 0, 40)
        f.Position = UDim2.new(0.5, -90, 0.76, 0)
        f.BackgroundColor3 = Colors.BG
        f.BackgroundTransparency = 0.1
        f.BorderSizePixel = 0
        f.Parent = sg
        MakeCorner(f, 10)
        MakeStroke(f, Colors.Primary, 1.5)
        
        local txt = Instance.new("TextLabel")
        txt.Size = UDim2.new(1, 0, 1, 0)
        txt.BackgroundTransparency = 1
        txt.Text = "🧪 ACTIVE POTION"
        txt.Font = Enum.Font.GothamBold
        txt.TextSize = 12
        txt.TextColor3 = Colors.Primary
        txt.Parent = f
        
        UIPanels.potion = sg
    else
        if UIPanels.potion then UIPanels.potion:Destroy(); UIPanels.potion = nil end
    end
end

local function ShowUnlockPanel(show)
    if show then
        if UIPanels.unlock then UIPanels.unlock:Destroy() end
        local sg = Instance.new("ScreenGui")
        sg.Name = "LawUnlockPanel"
        sg.ResetOnSpawn = false
        sg.Parent = LocalPlayer.PlayerGui
        
        local f = Instance.new("Frame")
        f.Size = UDim2.new(0, 200, 0, 90)
        f.Position = UDim2.new(0.5, -100, 0.7, 0)
        f.BackgroundColor3 = Colors.BG
        f.BackgroundTransparency = 0.1
        f.BorderSizePixel = 0
        f.Parent = sg
        MakeCorner(f, 12)
        MakeStroke(f, Colors.Primary, 1.5)
        
        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 30)
        title.BackgroundTransparency = 1
        title.Text = "🔓 UNLOCK BASE"
        title.Font = Enum.Font.GothamBold
        title.TextSize = 12
        title.TextColor3 = Colors.Primary
        title.Parent = f
        
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.8, 0, 0, 40)
        btn.Position = UDim2.new(0.1, 0, 0, 40)
        btn.BackgroundColor3 = Colors.Panel
        btn.Text = "UNLOCK"
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 14
        btn.TextColor3 = Colors.Text
        btn.Parent = f
        MakeCorner(btn, 8)
        MakeStroke(btn, Colors.Primary, 1.5)
        
        btn.MouseButton1Click:Connect(function()
            Tween(btn, {BackgroundColor3 = Colors.Primary}, 0.1)
            task.delay(0.15, function() Tween(btn, {BackgroundColor3 = Colors.Panel}, 0.1) end)
            DoUnlock()
        end)
        
        UIPanels.unlock = sg
    else
        if UIPanels.unlock then UIPanels.unlock:Destroy(); UIPanels.unlock = nil end
    end
end

local function ShowActionsPanel(show)
    if show then
        if UIPanels.actions then UIPanels.actions:Destroy() end
        local sg = Instance.new("ScreenGui")
        sg.Name = "LawActionsPanel"
        sg.ResetOnSpawn = false
        sg.Parent = LocalPlayer.PlayerGui
        
        local f = Instance.new("Frame")
        f.Size = UDim2.new(0, 220, 0, 230)
        f.Position = UDim2.new(0.5, -110, 0.5, -115)
        f.BackgroundColor3 = Colors.BG
        f.BackgroundTransparency = 0.1
        f.BorderSizePixel = 0
        f.Active = true
        f.Draggable = true
        f.Parent = sg
        MakeCorner(f, 12)
        MakeStroke(f, Colors.Primary, 1.5)
        
        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 35)
        title.BackgroundTransparency = 1
        title.Text = "⚡ ACTIONS"
        title.Font = Enum.Font.GothamBold
        title.TextSize = 14
        title.TextColor3 = Colors.Primary
        title.Parent = f
        
        local container = Instance.new("Frame")
        container.Size = UDim2.new(1, -16, 1, -45)
        container.Position = UDim2.new(0, 8, 0, 40)
        container.BackgroundTransparency = 1
        container.Parent = f
        
        local layout = Instance.new("UIListLayout")
        layout.Padding = UDim.new(0, 8)
        layout.Parent = container
        
        local buttons = {
            {"🔄 REJOIN", Rejoin},
            {"💀 KICK SELF", KickSelf},
            {"🏃 RAGDOLL", RagdollSelf},
            {"🔄 RESET", ResetChar},
        }
        
        for _, btnData in ipairs(buttons) do
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, 0, 0, 42)
            btn.BackgroundColor3 = Colors.Panel
            btn.Text = btnData[1]
            btn.Font = Enum.Font.GothamBold
            btn.TextSize = 13
            btn.TextColor3 = Colors.Text
            btn.Parent = container
            MakeCorner(btn, 8)
            MakeStroke(btn, Colors.Primary, 1)
            
            btn.MouseEnter:Connect(function()
                Tween(btn, {BackgroundColor3 = Color3.fromRGB(45, 45, 45)}, 0.1)
            end)
            btn.MouseLeave:Connect(function()
                Tween(btn, {BackgroundColor3 = Colors.Panel}, 0.1)
            end)
            btn.MouseButton1Click:Connect(function()
                Tween(btn, {BackgroundColor3 = Colors.Primary}, 0.08)
                task.delay(0.15, function() Tween(btn, {BackgroundColor3 = Colors.Panel}, 0.1) end)
                pcall(btnData[2])
            end)
        end
        
        UIPanels.actions = sg
    else
        if UIPanels.actions then UIPanels.actions:Destroy(); UIPanels.actions = nil end
    end
end

-- =============================================
-- CREATE MAIN MENU (DRAGGABLE)
-- =============================================
local MainMenu = nil
local LawButton = nil
local MenuVisible = true

local function CreateMainMenu()
    if MainMenu then MainMenu:Destroy() end
    
    local sg = Instance.new("ScreenGui")
    sg.Name = "LawHub"
    sg.ResetOnSpawn = false
    sg.Parent = LocalPlayer.PlayerGui
    
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 300, 0, 400)
    frame.Position = UDim2.new(0.5, -150, 0.5, -200)
    frame.AnchorPoint = Vector2.new(0.5, 0.5)
    frame.BackgroundColor3 = Colors.BG
    frame.BackgroundTransparency = 0.05
    frame.BorderSizePixel = 0
    frame.Active = true
    frame.Draggable = true
    frame.Parent = sg
    MakeCorner(frame, 12)
    MakeStroke(frame, Colors.Primary, 2)
    
    -- Title
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 50)
    title.BackgroundTransparency = 1
    title.Text = "LAW HUB"
    title.Font = Enum.Font.GothamBlack
    title.TextSize = 24
    title.TextColor3 = Colors.Primary
    title.Parent = frame
    
    -- Separator
    local line = Instance.new("Frame")
    line.Size = UDim2.new(0.9, 0, 0, 2)
    line.Position = UDim2.new(0.05, 0, 0, 52)
    line.BackgroundColor3 = Colors.Primary
    line.Parent = frame
    
    -- Scroll frame
    local scroll = Instance.new("ScrollingFrame")
    scroll.Size = UDim2.new(1, 0, 1, -75)
    scroll.Position = UDim2.new(0, 0, 0, 60)
    scroll.BackgroundTransparency = 1
    scroll.ScrollBarThickness = 3
    scroll.ScrollBarImageColor3 = Colors.Primary
    scroll.CanvasSize = UDim2.new(0, 0, 0, 280)
    scroll.Parent = frame
    
    local container = Instance.new("Frame")
    container.Size = UDim2.new(1, 0, 0, 280)
    container.BackgroundTransparency = 1
    container.Parent = scroll
    
    -- Toggle 1: Instant Steal
    CreateToggle(container, 5, "INSTANT STEAL", "Auto teleport + steal", "steal", function(on)
        Toggles.steal = on
        if on then
            StartStealLoop()
        else
            StopStealLoop()
        end
        ShowStealPanel(on)
    end)
    
    -- Toggle 2: Active Potion
    CreateToggle(container, 65, "ACTIVE POTION", "Auto use potion on steal", "potion", function(on)
        Toggles.potion = on
        if on then
            StartPotionWatcher()
        else
            StopPotionWatcher()
        end
        ShowPotionPanel(on)
    end)
    
    -- Toggle 3: Unlock Base
    CreateToggle(container, 125, "UNLOCK BASE", "Unlock other player's base", "unlock", function(on)
        Toggles.unlock = on
        ShowUnlockPanel(on)
    end)
    
    -- Toggle 4: Actions Panel
    CreateToggle(container, 185, "ACTIONS PANEL", "Rejoin, Kick, Ragdoll, Reset", "actions", function(on)
        Toggles.actions = on
        ShowActionsPanel(on)
    end)
    
    -- Footer
    local footer = Instance.new("TextLabel")
    footer.Size = UDim2.new(1, 0, 0, 22)
    footer.Position = UDim2.new(0, 0, 1, -24)
    footer.BackgroundTransparency = 1
    footer.Text = "LAW HUB"
    footer.Font = Enum.Font.Gotham
    footer.TextSize = 9
    footer.TextColor3 = Color3.fromRGB(100, 100, 100)
    footer.Parent = frame
    
    MainMenu = sg
end

-- =============================================
-- CREATE FLOATING LAW BUTTON
-- =============================================
local function CreateLawButton()
    if LawButton then LawButton:Destroy() end
    
    local btn = Instance.new("TextButton")
    btn.Name = "LawButton"
    btn.Size = UDim2.new(0, 70, 0, 32)
    btn.Position = UDim2.new(1, -80, 0, 10)
    btn.BackgroundColor3 = Colors.BG
    btn.BackgroundTransparency = 0.2
    btn.Text = "LAW"
    btn.TextColor3 = Colors.Primary
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 16
    btn.BorderSizePixel = 0
    btn.Parent = LocalPlayer.PlayerGui
    MakeCorner(btn, 8)
    MakeStroke(btn, Colors.Primary, 2)
    
    btn.MouseEnter:Connect(function()
        Tween(btn, {BackgroundTransparency = 0, Size = UDim2.new(0, 75, 0, 35)}, 0.15)
    end)
    btn.MouseLeave:Connect(function()
        Tween(btn, {BackgroundTransparency = 0.2, Size = UDim2.new(0, 70, 0, 32)}, 0.15)
    end)
    
    btn.MouseButton1Click:Connect(function()
        MenuVisible = not MenuVisible
        if MainMenu then
            MainMenu.Visible = MenuVisible
        end
    end)
    
    LawButton = btn
end

-- =============================================
-- INITIALIZE
-- =============================================
CreateLawButton()
CreateMainMenu()

print("✅ LAW HUB LOADED")
print("📌 Click 'LAW' button to open menu")
print("⚡ Drag the menu by its title bar")
