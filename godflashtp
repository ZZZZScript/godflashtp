-- by @atlanta.rar

-- // [1] SERVICES //
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ProximityPromptService = game:GetService("ProximityPromptService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer

-- // [2] CONFIGURATION //
local CONFIG = {
    Gui = {
        ScreenGuiName = "GodHub",
        ResetOnSpawn = false,
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling,
    },
    MainFrame = {
        Name = "Main",
        Size = UDim2.new(0, 200, 0, 126),
        Position = UDim2.new(0.5, -100, 0.5, -59),
        BackgroundColor3 = Color3.fromRGB(10, 15, 30),
        BackgroundTransparency = 0.1,
        BorderSizePixel = 0,
        Active = true,
    },
    TitleBar = {
        Height = 26,
        Text = "discord.gg/godhub",
        TextColor3 = Color3.fromRGB(100, 200, 255),
        TextSize = 12,
    },
    Toggle = {
        Text = "Auto Flash",
        TextColor3 = Color3.fromRGB(180, 220, 255),
        TextSize = 11,
        OffColor = Color3.fromRGB(255, 100, 100),
        OnColor = Color3.fromRGB(150, 255, 150),
        DefaultState = true,
    },
    Slider = {
        Label = "Trigger Percent",
        DefaultPercent = 92,
        BarColor = Color3.fromRGB(35, 40, 50),
        FillColor = Color3.fromRGB(100, 200, 255),
        KnobColor = Color3.fromRGB(255, 255, 255),
    },
    Accent = {
        StrokeColor = Color3.fromRGB(100, 200, 255),
        GradientRotationSpeed = 1,
    },
}

-- // [3] VARIABLES //
local ScreenGui, MainFrame
local ToggleFrame, ToggleLabel, StatusLabel, ToggleButton
local SliderContainer, SliderLeftLabel, SliderRightLabel, SliderBar, SliderFill, SliderKnob

local flashEnabled = CONFIG.Toggle.DefaultState
local sliderValue = CONFIG.Slider.DefaultPercent / 100
local draggingSlider = false
local manualOverride = false
local triggerBump = false
local activeTriggers = {}

-- // [4] UTILITY //
local function createCorner(parent, radius)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, radius or 6)
    c.Parent = parent
end

local function createStroke(parent, color, thickness)
    local s = Instance.new("UIStroke")
    s.Color = color or Color3.fromRGB(100, 200, 255)
    s.Thickness = thickness or 1.5
    s.Parent = parent
    return s
end

local function createGradient(parent)
    local g = Instance.new("UIGradient")
    g.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0,   Color3.fromRGB(0, 0, 0)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(100, 200, 255)),
        ColorSequenceKeypoint.new(1,   Color3.fromRGB(0, 0, 0)),
    }
    g.Parent = parent
    return g
end

local function makeDraggable(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
        end
    end)
    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
            dragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end)
    frame.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
end

local function getPingBase(ping)
    if ping <= 30 then return 0.91
    elseif ping <= 70 then return 0.92
    elseif ping <= 120 then return 0.93
    else return 0.94 end
end

-- // [5] GUI CREATION //
local function createGUI()
    ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = CONFIG.Gui.ScreenGuiName
    ScreenGui.ResetOnSpawn = CONFIG.Gui.ResetOnSpawn
    ScreenGui.ZIndexBehavior = CONFIG.Gui.ZIndexBehavior
    ScreenGui.Parent = CoreGui

    MainFrame = Instance.new("Frame")
    MainFrame.Name = CONFIG.MainFrame.Name
    MainFrame.Size = CONFIG.MainFrame.Size
    MainFrame.Position = CONFIG.MainFrame.Position
    MainFrame.BackgroundColor3 = CONFIG.MainFrame.BackgroundColor3
    MainFrame.BackgroundTransparency = CONFIG.MainFrame.BackgroundTransparency
    MainFrame.BorderSizePixel = CONFIG.MainFrame.BorderSizePixel
    MainFrame.Active = CONFIG.MainFrame.Active
    MainFrame.Parent = ScreenGui
    createCorner(MainFrame, 6)
    createGradient(createStroke(MainFrame, CONFIG.Accent.StrokeColor, 1.5))

    -- Title bar
    local TitleBar = Instance.new("Frame")
    TitleBar.Size = UDim2.new(1, 0, 0, CONFIG.TitleBar.Height)
    TitleBar.BackgroundTransparency = 1
    TitleBar.Parent = MainFrame

    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Size = UDim2.new(1, 0, 1, 0)
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Text = CONFIG.TitleBar.Text
    TitleLabel.TextColor3 = CONFIG.TitleBar.TextColor3
    TitleLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
    TitleLabel.TextSize = CONFIG.TitleBar.TextSize
    TitleLabel.Parent = TitleBar

    -- Toggle row
    ToggleFrame = Instance.new("Frame")
    ToggleFrame.Size = UDim2.new(1, -20, 0, 26)
    ToggleFrame.Position = UDim2.new(0, 10, 0, 30)
    ToggleFrame.BackgroundColor3 = Color3.fromRGB(25, 35, 45)
    ToggleFrame.Parent = MainFrame
    createCorner(ToggleFrame, 4)
    createStroke(ToggleFrame, CONFIG.Accent.StrokeColor, 1)

    ToggleLabel = Instance.new("TextLabel")
    ToggleLabel.Size = UDim2.new(1, -50, 1, 0)
    ToggleLabel.Position = UDim2.new(0, 8, 0, 0)
    ToggleLabel.BackgroundTransparency = 1
    ToggleLabel.Text = CONFIG.Toggle.Text
    ToggleLabel.TextColor3 = CONFIG.Toggle.TextColor3
    ToggleLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
    ToggleLabel.TextSize = CONFIG.Toggle.TextSize
    ToggleLabel.TextXAlignment = Enum.TextXAlignment.Left
    ToggleLabel.Parent = ToggleFrame

    StatusLabel = Instance.new("TextLabel")
    StatusLabel.Size = UDim2.new(0, 40, 1, 0)
    StatusLabel.Position = UDim2.new(1, -8, 0, 0)
    StatusLabel.AnchorPoint = Vector2.new(1, 0)
    StatusLabel.BackgroundTransparency = 1
    StatusLabel.Text = flashEnabled and "ON" or "OFF"
    StatusLabel.TextColor3 = flashEnabled and CONFIG.Toggle.OnColor or CONFIG.Toggle.OffColor
    StatusLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
    StatusLabel.TextSize = CONFIG.Toggle.TextSize
    StatusLabel.TextXAlignment = Enum.TextXAlignment.Right
    StatusLabel.Parent = ToggleFrame

    ToggleButton = Instance.new("TextButton")
    ToggleButton.Size = UDim2.new(1, 0, 1, 0)
    ToggleButton.BackgroundTransparency = 1
    ToggleButton.Text = ""
    ToggleButton.Parent = ToggleFrame

    -- Slider section
    SliderContainer = Instance.new("Frame")
    SliderContainer.Size = UDim2.new(1, -20, 0, 38)
    SliderContainer.Position = UDim2.new(0, 10, 0, 64)
    SliderContainer.BackgroundTransparency = 1
    SliderContainer.Parent = MainFrame

    SliderLeftLabel = Instance.new("TextLabel")
    SliderLeftLabel.Size = UDim2.new(1, -50, 0, 16)
    SliderLeftLabel.Position = UDim2.new(0, 0, 0, 0)
    SliderLeftLabel.BackgroundTransparency = 1
    SliderLeftLabel.Text = CONFIG.Slider.Label
    SliderLeftLabel.TextColor3 = Color3.fromRGB(180, 220, 255)
    SliderLeftLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
    SliderLeftLabel.TextSize = 11
    SliderLeftLabel.TextXAlignment = Enum.TextXAlignment.Left
    SliderLeftLabel.Parent = SliderContainer

    SliderRightLabel = Instance.new("TextLabel")
    SliderRightLabel.Size = UDim2.new(0, 40, 0, 16)
    SliderRightLabel.Position = UDim2.new(1, -40, 0, 0)
    SliderRightLabel.BackgroundTransparency = 1
    SliderRightLabel.Text = CONFIG.Slider.DefaultPercent .. "%"
    SliderRightLabel.TextColor3 = Color3.fromRGB(180, 220, 255)
    SliderRightLabel.FontFace = Font.new("rbxasset://fonts/families/GothamSSm.json", Enum.FontWeight.Bold, Enum.FontStyle.Normal)
    SliderRightLabel.TextSize = 11
    SliderRightLabel.TextXAlignment = Enum.TextXAlignment.Right
    SliderRightLabel.Parent = SliderContainer

    SliderBar = Instance.new("Frame")
    SliderBar.Size = UDim2.new(1, 0, 0, 4)
    SliderBar.Position = UDim2.new(0, 0, 0, 24)
    SliderBar.BackgroundColor3 = CONFIG.Slider.BarColor
    SliderBar.Parent = SliderContainer
    createCorner(SliderBar, 4)

    SliderFill = Instance.new("Frame")
    SliderFill.Size = UDim2.new(sliderValue, 0, 1, 0)
    SliderFill.BackgroundColor3 = CONFIG.Slider.FillColor
    SliderFill.Parent = SliderBar
    createCorner(SliderFill, 4)

    SliderKnob = Instance.new("Frame")
    SliderKnob.Size = UDim2.new(0, 10, 0, 10)
    SliderKnob.Position = UDim2.new(sliderValue, -5, 0.5, -5)
    SliderKnob.BackgroundColor3 = CONFIG.Slider.KnobColor
    SliderKnob.Parent = SliderBar
    createCorner(SliderKnob, 10)
end

-- // [6] FUNCTIONALITY //
local function updateSliderVisual(v)
    sliderValue = v
    SliderFill.Size = UDim2.new(v, 0, 1, 0)
    SliderKnob.Position = UDim2.new(v, -5, 0.5, -5)
    SliderRightLabel.Text = math.floor(v * 100) .. "%"
end

local function setupSlider()
    SliderBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            manualOverride = true
            draggingSlider = true
            updateSliderVisual(math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1))
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if draggingSlider and input.UserInputType == Enum.UserInputType.MouseMovement then
            updateSliderVisual(math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1))
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            draggingSlider = false
        end
    end)
end

local function setupFunctionality()
    makeDraggable(MainFrame)
    setupSlider()

    -- Spinning gradient accent
    if CONFIG.Accent.GradientRotationSpeed ~= 0 then
        local stroke = MainFrame:FindFirstChildWhichIsA("UIStroke")
        local grad = stroke and stroke:FindFirstChildWhichIsA("UIGradient")
        if grad then
            RunService.Heartbeat:Connect(function(dt)
                grad.Rotation = (grad.Rotation + CONFIG.Accent.GradientRotationSpeed * 60 * dt) % 360
            end)
        end
    end

    -- Toggle
    ToggleButton.MouseButton1Click:Connect(function()
        flashEnabled = not flashEnabled
        StatusLabel.Text = flashEnabled and "ON" or "OFF"
        StatusLabel.TextColor3 = flashEnabled and CONFIG.Toggle.OnColor or CONFIG.Toggle.OffColor
    end)

    -- Auto ping-based slider
    RunService.Heartbeat:Connect(function()
        if not manualOverride then
            local ping = LocalPlayer:GetNetworkPing() * 1000
            local base = getPingBase(ping)
            updateSliderVisual(triggerBump and base + 0.01 or base)
        end
    end)

    -- Flash logic
    ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt)
        if not flashEnabled then return end
        if activeTriggers[prompt] then return end
        activeTriggers[prompt] = true
        local start = os.clock()
        local fired = false

        local conn
        conn = RunService.PreRender:Connect(function()
            if not prompt or not prompt.Parent then
                conn:Disconnect()
                activeTriggers[prompt] = nil
                return
            end
            if not fired and (os.clock() - start) / prompt.HoldDuration >= sliderValue then
                fired = true
                conn:Disconnect()
                activeTriggers[prompt] = nil
                triggerBump = not triggerBump
                local char = LocalPlayer.Character
                local tool = char and char:FindFirstChildOfClass("Tool")
                if tool then tool:Activate() end
            end
        end)

        prompt.PromptButtonHoldEnded:Connect(function()
            if not fired then
                conn:Disconnect()
                activeTriggers[prompt] = nil
            end
        end)
    end)
end

-- // [7] INIT //
createGUI()
setupFunctionality()
print("[GodHub] Loaded!")
