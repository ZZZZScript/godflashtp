-- =============================================
-- LAW HUB - WORKING VERSION
-- =============================================

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RS = game:GetService("RunService")
local TS = game:GetService("TweenService")
local TP = game:GetService("TeleportService")
local LP = Players.LocalPlayer

-- CORES
local Yellow = Color3.fromRGB(255, 200, 0)
local Black = Color3.fromRGB(15, 15, 15)
local Dark = Color3.fromRGB(28, 28, 28)

-- ESTADOS
local InstantStealOn = false
local PotionStealOn = false
local StealSpeedOn = false
local UnlockBaseOn = false
local ActionsOn = false

local currentKeybind = Enum.KeyCode.R
local currentSpeed = 28
local listening = false
local teleporting = false
local fflagsSet = false

local Root = nil
local Hum = nil
local StealThread = nil
local SpeedThread = nil

-- =============================================
-- FFLAGS (RESET)
-- =============================================
local fflags = {
    GameNetPVHeaderRotationalVelocityZeroCutoffExponent = -5000,
    LargeReplicatorWrite5 = true,
    LargeReplicatorEnabled9 = true,
    AngularVelociryLimit = 360,
    TimestepArbiterVelocityCriteriaThresholdTwoDt = 2147483646,
    S2PhysicsSenderRate = 15000,
    DisableDPIScale = true,
    MaxDataPacketPerSend = 2147483647,
    PhysicsSenderMaxBandwidthBps = 20000,
    TimestepArbiterHumanoidLinearVelThreshold = 21,
    MaxMissedWorldStepsRemembered = -2147483648,
    PlayerHumanoidPropertyUpdateRestrict = true,
    SimDefaultHumanoidTimestepMultiplier = 0,
    StreamJobNOUVolumeLengthCap = 2147483647,
    DebugSendDistInSteps = -2147483648,
    GameNetDontSendRedundantNumTimes = 1,
    CheckPVLinearVelocityIntegrateVsDeltaPositionThresholdPercent = 1,
    CheckPVDifferencesForInterpolationMinVelThresholdStudsPerSecHundredth = 1,
    LargeReplicatorSerializeRead3 = true,
    ReplicationFocusNouExtentsSizeCutoffForPauseStuds = 2147483647,
    CheckPVCachedVelThresholdPercent = 10,
    CheckPVDifferencesForInterpolationMinRotVelThresholdRadsPerSecHundredth = 1,
    GameNetDontSendRedundantDeltaPositionMillionth = 1,
    InterpolationFrameVelocityThresholdMillionth = 5,
    StreamJobNOUVolumeCap = 2147483647,
    InterpolationFrameRotVelocityThresholdMillionth = 5,
    CheckPVCachedRotVelThresholdPercent = 10,
    WorldStepMax = 30,
    InterpolationFramePositionThresholdMillionth = 5,
    TimestepArbiterHumanoidTurningVelThreshold = 1,
    SimOwnedNOUCountThresholdMillionth = 2147483647,
    GameNetPVHeaderLinearVelocityZeroCutoffExponent = -5000,
    NextGenReplicatorEnabledWrite4 = true,
    TimestepArbiterOmegaThou = 1073741823,
    MaxAcceptableUpdateDelay = 1,
    LargeReplicatorSerializeWrite4 = true
}

local function SetFFLags()
    for k, v in pairs(fflags) do
        pcall(function() setfflag(k, tostring(v)) end)
    end
end

-- =============================================
-- UTILS
-- =============================================
local function Tween(obj, prop, val, time)
    TS:Create(obj, TweenInfo.new(time or 0.2), {[prop] = val}):Play()
end

local function Corner(obj, r)
    Instance.new("UICorner", obj).CornerRadius = UDim.new(0, r or 8)
end

local function Stroke(obj, color, thick)
    local s = Instance.new("UIStroke", obj)
    s.Color = color or Yellow
    s.Thickness = thick or 1.5
end

-- =============================================
-- CHARACTER
-- =============================================
LP.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    Root = char:FindFirstChild("HumanoidRootPart")
    Hum = char:FindFirstChildOfClass("Humanoid")
end)

local char = LP.Character or LP.CharacterAdded:Wait()
Root = char:FindFirstChild("HumanoidRootPart")
Hum = char:FindFirstChildOfClass("Humanoid")

-- =============================================
-- ADMIN COMMANDS
-- =============================================
local function GetAdminFrames()
    local ap = LP.PlayerGui:FindFirstChild("AdminPanel")
    if not ap then return nil, nil end
    local pnl = ap:FindFirstChild("AdminPanel")
    if not pnl then return nil, nil end
    local cont = pnl:FindFirstChild("Content")
    local prof = pnl:FindFirstChild("Profiles")
    if not cont or not prof then return nil, nil end
    return cont:FindFirstChild("ScrollingFrame"), prof:FindFirstChild("ScrollingFrame")
end

local function FireCmd(btn)
    local ok, conns = pcall(getconnections, btn.Activated)
    if ok and type(conns) == "table" then
        for _, c in ipairs(conns) do
            if type(c.Function) == "function" then
                task.spawn(c.Function)
            end
        end
    end
end

local function RunCmd(cmd, target)
    local cmdF, profF = GetAdminFrames()
    if not cmdF or not profF then return end
    local profBtn = profF:FindFirstChild(target.Name)
    local cmdBtn = cmdF:FindFirstChild(cmd)
    if not profBtn or not cmdBtn then return end
    FireCmd(profBtn)
    task.wait(0.05)
    FireCmd(cmdBtn)
    task.wait(0.05)
    FireCmd(profBtn)
    task.wait(0.05)
    FireCmd(cmdBtn)
end

-- =============================================
-- TELEPORT FUNCTIONS
-- =============================================
local function GetMyPlot()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    for _, p in pairs(plots:GetChildren()) do
        local sign = p:FindFirstChild("PlotSign")
        if sign and sign:FindFirstChild("YourBase") and sign.YourBase.Enabled then
            return p
        end
    end
    return nil
end

local function TeleportTo(pos)
    if Root then
        Root.CFrame = CFrame.new(pos)
        Root.Velocity = Vector3.zero
    end
end

local function EquipCarpet()
    local char = LP.Character
    local bp = LP:FindFirstChild("Backpack")
    if not char or not bp then return end
    for _, t in pairs(char:GetChildren()) do
        if t:IsA("Tool") then t.Parent = bp end
    end
    local carpet = bp:FindFirstChild("Flying Carpet")
    if carpet and Hum then Hum:EquipTool(carpet) end
end

local function UseGiantPotion()
    local bp = LP:FindFirstChild("Backpack")
    local char = LP.Character
    if not bp or not char then return end
    local pot = bp:FindFirstChild("Giant Potion")
    if pot then
        pot.Parent = char
        pcall(function() pot:Activate() end)
    end
end

local function DoReset()
    if not fflagsSet then
        for i = 1, 10 do SetFFLags() end
        fflagsSet = true
    end
    if LP.Character then LP.Character:Destroy() end
end

local function DoTeleportAndSteal()
    if teleporting then return end
    teleporting = true
    
    if PotionStealOn then
        UseGiantPotion()
    end
    
    local plot = GetMyPlot()
    if not plot then
        teleporting = false
        return
    end
    
    EquipCarpet()
    task.wait(0.1)
    
    local order = plot:GetAttribute("Order")
    if order == 2 then
        TeleportTo(Vector3.new(-368, -7, 69))
        task.wait(0.1)
        TeleportTo(Vector3.new(-336, -5, 100))
        task.wait(0.1)
        TeleportTo(Vector3.new(-352, -7, 76))
    else
        TeleportTo(Vector3.new(-368, -7, 42))
        task.wait(0.1)
        TeleportTo(Vector3.new(-336, -5, 20))
        task.wait(0.1)
        TeleportTo(Vector3.new(-353, -7, 44))
    end
    
    task.wait(0.2)
    
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP then
            RunCmd("steal", p)
            break
        end
    end
    
    task.wait(0.5)
    teleporting = false
end

local function StartSteal()
    if StealThread then StealThread:Disconnect() end
    StealThread = RS.Heartbeat:Connect(function()
        if InstantStealOn and Hum and Hum.Health > 0 then
            DoTeleportAndSteal()
            task.wait(1.5)
        end
    end)
end

local function StopSteal()
    if StealThread then StealThread:Disconnect(); StealThread = nil end
end

local function StartSpeed()
    if SpeedThread then SpeedThread:Disconnect() end
    SpeedThread = RS.Heartbeat:Connect(function()
        if InstantStealOn and StealSpeedOn and Hum then
            local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
            local dir = Hum.MoveDirection
            if hrp and dir.Magnitude > 0 then
                hrp.Velocity = Vector3.new(dir.X * currentSpeed, hrp.Velocity.Y, dir.Z * currentSpeed)
            end
        end
    end)
end

local function StopSpeed()
    if SpeedThread then SpeedThread:Disconnect(); SpeedThread = nil end
end

-- =============================================
-- UNLOCK BASE
-- =============================================
local function FindOtherPlot()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    for _, p in pairs(plots:GetChildren()) do
        local sign = p:FindFirstChild("PlotSign")
        if sign then
            local yb = sign:FindFirstChild("YourBase")
            if not (yb and yb.Enabled) then
                return p
            end
        end
    end
    return nil
end

local function DoUnlock()
    local plot = FindOtherPlot()
    if not plot then return end
    for _, o in pairs(plot:GetDescendants()) do
        if o:IsA("ProximityPrompt") and o.Enabled then
            local txt = (o.ActionText or ""):lower()
            if txt:find("unlock") then
                local old = o.MaxActivationDistance
                o.MaxActivationDistance = 9999
                pcall(function() fireproximityprompt(o) end)
                task.delay(0.3, function() o.MaxActivationDistance = old end)
                break
            end
        end
    end
end

-- =============================================
-- ACTIONS
-- =============================================
local function Ragdoll()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP then RunCmd("ragdoll", p); break end
    end
end

local function Balloon()
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LP then RunCmd("balloon", p); break end
    end
end

local function Rejoin() TP:Teleport(game.PlaceId, LP) end
local function Kick() LP:Kick("Law Hub") end
local function Reset() if LP.Character then LP.Character:Destroy() end end

-- =============================================
-- UI PANELS
-- =============================================
local StealPanel = nil
local UnlockPanel = nil
local ActionsPanel = nil

local function CreateStealPanel()
    if StealPanel then StealPanel:Destroy() end
    
    local sg = Instance.new("ScreenGui")
    sg.Name = "LawSteal"
    sg.ResetOnSpawn = false
    sg.Parent = LP.PlayerGui
    
    local f = Instance.new("Frame")
    f.Size = UDim2.new(0, 260, 0, 350)
    f.Position = UDim2.new(0.5, -130, 0.3, 0)
    f.BackgroundColor3 = Black
    f.BackgroundTransparency = 0.1
    f.BorderSizePixel = 0
    f.Active = true
    f.Draggable = true
    f.Parent = sg
    Corner(f, 12)
    Stroke(f, Yellow, 1.5)
    
    -- Titulo
    local t = Instance.new("TextLabel", f)
    t.Size = UDim2.new(1, 0, 0, 35)
    t.BackgroundTransparency = 1
    t.Text = "Instant Steal TP"
    t.Font = Enum.Font.GothamBold
    t.TextSize = 14
    t.TextColor3 = Yellow
    
    local sub = Instance.new("TextLabel", f)
    sub.Size = UDim2.new(1, 0, 0, 20)
    sub.Position = UDim2.new(0, 0, 0, 32)
    sub.BackgroundTransparency = 1
    sub.Text = "E/R Teleport Helper"
    sub.Font = Enum.Font.Gotham
    sub.TextSize = 10
    sub.TextColor3 = Color3.fromRGB(150,150,150)
    
    -- Keybind
    local kbLabel = Instance.new("TextLabel", f)
    kbLabel.Size = UDim2.new(1, -16, 0, 15)
    kbLabel.Position = UDim2.new(0, 8, 0, 58)
    kbLabel.BackgroundTransparency = 1
    kbLabel.Text = "Keybind"
    kbLabel.Font = Enum.Font.Gotham
    kbLabel.TextSize = 10
    kbLabel.TextColor3 = Color3.fromRGB(150,150,150)
    kbLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local kbBtn = Instance.new("TextButton", f)
    kbBtn.Size = UDim2.new(1, -16, 0, 28)
    kbBtn.Position = UDim2.new(0, 8, 0, 73)
    kbBtn.BackgroundColor3 = Dark
    kbBtn.Text = "[ R ]"
    kbBtn.Font = Enum.Font.GothamBold
    kbBtn.TextSize = 12
    kbBtn.TextColor3 = Color3.fromRGB(255,255,255)
    Corner(kbBtn, 6)
    Stroke(kbBtn, Yellow, 1)
    
    -- Teleport
    local teleBtn = Instance.new("TextButton", f)
    teleBtn.Size = UDim2.new(1, -16, 0, 35)
    teleBtn.Position = UDim2.new(0, 8, 0, 108)
    teleBtn.BackgroundColor3 = Dark
    teleBtn.Text = "Teleport"
    teleBtn.Font = Enum.Font.GothamBold
    teleBtn.TextSize = 13
    teleBtn.TextColor3 = Color3.fromRGB(255,255,255)
    Corner(teleBtn, 6)
    Stroke(teleBtn, Yellow, 1)
    
    -- Active Potion (Reset)
    local activeBtn = Instance.new("TextButton", f)
    activeBtn.Size = UDim2.new(1, -16, 0, 35)
    activeBtn.Position = UDim2.new(0, 8, 0, 150)
    activeBtn.BackgroundColor3 = Dark
    activeBtn.Text = "Active Potion"
    activeBtn.Font = Enum.Font.GothamBold
    activeBtn.TextSize = 13
    activeBtn.TextColor3 = Color3.fromRGB(255,255,255)
    Corner(activeBtn, 6)
    Stroke(activeBtn, Yellow, 1)
    
    -- Separador
    local sep = Instance.new("Frame", f)
    sep.Size = UDim2.new(1, -16, 0, 1)
    sep.Position = UDim2.new(0, 8, 0, 195)
    sep.BackgroundColor3 = Yellow
    sep.BackgroundTransparency = 0.5
    
    -- Options
    local optLabel = Instance.new("TextLabel", f)
    optLabel.Size = UDim2.new(1, -16, 0, 15)
    optLabel.Position = UDim2.new(0, 8, 0, 202)
    optLabel.BackgroundTransparency = 1
    optLabel.Text = "Options"
    optLabel.Font = Enum.Font.Gotham
    optLabel.TextSize = 10
    optLabel.TextColor3 = Color3.fromRGB(150,150,150)
    optLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    -- Potion Steal Toggle
    local potRow = Instance.new("Frame", f)
    potRow.Size = UDim2.new(1, -16, 0, 30)
    potRow.Position = UDim2.new(0, 8, 0, 218)
    potRow.BackgroundColor3 = Dark
    potRow.BackgroundTransparency = 0.5
    Corner(potRow, 6)
    
    local potLabel = Instance.new("TextLabel", potRow)
    potLabel.Size = UDim2.new(1, -50, 1, 0)
    potLabel.Position = UDim2.new(0, 8, 0, 0)
    potLabel.BackgroundTransparency = 1
    potLabel.Text = "Potion Steal"
    potLabel.Font = Enum.Font.GothamBold
    potLabel.TextSize = 11
    potLabel.TextColor3 = Color3.fromRGB(255,255,255)
    potLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local potSw = Instance.new("Frame", potRow)
    potSw.Size = UDim2.new(0, 36, 0, 18)
    potSw.Position = UDim2.new(1, -44, 0.5, -9)
    potSw.BackgroundColor3 = Color3.fromRGB(50,50,50)
    Corner(potSw, 9)
    
    local potKnob = Instance.new("Frame", potSw)
    potKnob.Size = UDim2.new(0, 14, 0, 14)
    potKnob.Position = UDim2.new(0, 2, 0.5, -7)
    potKnob.BackgroundColor3 = Color3.fromRGB(150,150,150)
    Corner(potKnob, 7)
    
    -- Steal Speed Toggle
    local spdRow = Instance.new("Frame", f)
    spdRow.Size = UDim2.new(1, -16, 0, 30)
    spdRow.Position = UDim2.new(0, 8, 0, 252)
    spdRow.BackgroundColor3 = Dark
    spdRow.BackgroundTransparency = 0.5
    Corner(spdRow, 6)
    
    local spdLabel = Instance.new("TextLabel", spdRow)
    spdLabel.Size = UDim2.new(1, -50, 1, 0)
    spdLabel.Position = UDim2.new(0, 8, 0, 0)
    spdLabel.BackgroundTransparency = 1
    spdLabel.Text = "Steal Speed"
    spdLabel.Font = Enum.Font.GothamBold
    spdLabel.TextSize = 11
    spdLabel.TextColor3 = Color3.fromRGB(255,255,255)
    spdLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local spdSw = Instance.new("Frame", spdRow)
    spdSw.Size = UDim2.new(0, 36, 0, 18)
    spdSw.Position = UDim2.new(1, -44, 0.5, -9)
    spdSw.BackgroundColor3 = Color3.fromRGB(50,50,50)
    Corner(spdSw, 9)
    
    local spdKnob = Instance.new("Frame", spdSw)
    spdKnob.Size = UDim2.new(0, 14, 0, 14)
    spdKnob.Position = UDim2.new(0, 2, 0.5, -7)
    spdKnob.BackgroundColor3 = Color3.fromRGB(150,150,150)
    Corner(spdKnob, 7)
    
    -- Slider
    local sldFrame = Instance.new("Frame", f)
    sldFrame.Size = UDim2.new(1, -16, 0, 35)
    sldFrame.Position = UDim2.new(0, 8, 0, 288)
    sldFrame.BackgroundTransparency = 1
    
    local sldLabel = Instance.new("TextLabel", sldFrame)
    sldLabel.Size = UDim2.new(1, 0, 0, 15)
    sldLabel.BackgroundTransparency = 1
    sldLabel.Text = "Speed: 28"
    sldLabel.Font = Enum.Font.Gotham
    sldLabel.TextSize = 10
    sldLabel.TextColor3 = Color3.fromRGB(255,255,255)
    sldLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local sldBar = Instance.new("Frame", sldFrame)
    sldBar.Size = UDim2.new(1, 0, 0, 4)
    sldBar.Position = UDim2.new(0, 0, 0, 20)
    sldBar.BackgroundColor3 = Dark
    Corner(sldBar, 2)
    
    local sldFill = Instance.new("Frame", sldBar)
    sldFill.Size = UDim2.new(0.2, 0, 1, 0)
    sldFill.BackgroundColor3 = Yellow
    Corner(sldFill, 2)
    
    local sldKnob = Instance.new("Frame", sldBar)
    sldKnob.Size = UDim2.new(0, 12, 0, 12)
    sldKnob.Position = UDim2.new(0.2, -6, 0.5, -6)
    sldKnob.BackgroundColor3 = Color3.fromRGB(255,255,255)
    Corner(sldKnob, 6)
    
    -- FUNÇÕES
    local function updateSlider(x)
        local percent = math.clamp((x - sldBar.AbsolutePosition.X) / sldBar.AbsoluteSize.X, 0, 1)
        sldFill.Size = UDim2.new(percent, 0, 1, 0)
        sldKnob.Position = UDim2.new(percent, -6, 0.5, -6)
        currentSpeed = math.floor(26 + (36 - 26) * percent)
        sldLabel.Text = "Speed: " .. currentSpeed
    end
    
    -- Keybind
    kbBtn.MouseButton1Click:Connect(function()
        if listening then return end
        listening = true
        kbBtn.Text = "Press any key..."
        local conn
        conn = UIS.InputBegan:Connect(function(input)
            if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
            conn:Disconnect()
            currentKeybind = input.KeyCode
            kbBtn.Text = "[ " .. input.KeyCode.Name .. " ]"
            listening = false
        end)
    end)
    
    -- Teleport
    teleBtn.MouseButton1Click:Connect(function()
        DoTeleportAndSteal()
        Tween(teleBtn, "BackgroundColor3", Yellow, 0.1)
        task.delay(0.2, function() Tween(teleBtn, "BackgroundColor3", Dark, 0.1) end)
    end)
    
    -- Active Potion (Reset)
    activeBtn.MouseButton1Click:Connect(function()
        DoReset()
        Tween(activeBtn, "BackgroundColor3", Yellow, 0.1)
        task.delay(0.2, function() Tween(activeBtn, "BackgroundColor3", Dark, 0.1) end)
    end)
    
    -- Potion Toggle
    potSw.MouseButton1Click:Connect(function()
        PotionStealOn = not PotionStealOn
        if PotionStealOn then
            potSw.BackgroundColor3 = Yellow
            potKnob.Position = UDim2.new(1, -16, 0.5, -7)
            potKnob.BackgroundColor3 = Color3.fromRGB(255,255,255)
        else
            potSw.BackgroundColor3 = Color3.fromRGB(50,50,50)
            potKnob.Position = UDim2.new(0, 2, 0.5, -7)
            potKnob.BackgroundColor3 = Color3.fromRGB(150,150,150)
        end
    end)
    
    -- Speed Toggle
    spdSw.MouseButton1Click:Connect(function()
        StealSpeedOn = not StealSpeedOn
        if StealSpeedOn then
            spdSw.BackgroundColor3 = Yellow
            spdKnob.Position = UDim2.new(1, -16, 0.5, -7)
            spdKnob.BackgroundColor3 = Color3.fromRGB(255,255,255)
            if InstantStealOn then StartSpeed() end
        else
            spdSw.BackgroundColor3 = Color3.fromRGB(50,50,50)
            spdKnob.Position = UDim2.new(0, 2, 0.5, -7)
            spdKnob.BackgroundColor3 = Color3.fromRGB(150,150,150)
            StopSpeed()
        end
    end)
    
    -- Slider
    local dragging = false
    sldKnob.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true end
    end)
    UIS.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)
    UIS.InputChanged:Connect(function(i)
        if dragging and i.UserInputType == Enum.UserInputType.MouseMovement then
            updateSlider(i.Position.X)
        end
    end)
    sldBar.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then
            updateSlider(i.Position.X)
        end
    end)
    
    -- Keybind global
    UIS.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.Keyboard and i.KeyCode == currentKeybind then
            DoTeleportAndSteal()
        end
    end)
    
    StealPanel = sg
    return sg
end

local function CreateUnlockUI()
    if UnlockPanel then UnlockPanel:Destroy() end
    
    local sg = Instance.new("ScreenGui")
    sg.Name = "LawUnlock"
    sg.ResetOnSpawn = false
    sg.Parent = LP.PlayerGui
    
    local f = Instance.new("Frame")
    f.Size = UDim2.new(0, 220, 0, 100)
    f.Position = UDim2.new(0.5, -110, 0.65, 0)
    f.BackgroundColor3 = Black
    f.BackgroundTransparency = 0.1
    f.BorderSizePixel = 0
    f.Active = true
    f.Draggable = true
    f.Parent = sg
    Corner(f, 12)
    Stroke(f, Yellow, 1.5)
    
    local t = Instance.new("TextLabel", f)
    t.Size = UDim2.new(1, 0, 0, 35)
    t.BackgroundTransparency = 1
    t.Text = "UNLOCK BASE"
    t.Font = Enum.Font.GothamBold
    t.TextSize = 13
    t.TextColor3 = Yellow
    
    local c = Instance.new("Frame", f)
    c.Size = UDim2.new(1, -16, 1, -45)
    c.Position = UDim2.new(0, 8, 0, 40)
    c.BackgroundTransparency = 1
    
    local layout = Instance.new("UIListLayout", c)
    layout.Padding = UDim.new(0, 10)
    layout.FillDirection = Enum.FillDirection.Horizontal
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    
    for i = 1, 3 do
        local btn = Instance.new("TextButton", c)
        btn.Size = UDim2.new(0, 55, 0, 45)
        btn.BackgroundColor3 = Dark
        btn.Text = tostring(i)
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 18
        btn.TextColor3 = Color3.fromRGB(255,255,255)
        Corner(btn, 8)
        Stroke(btn, Yellow, 1)
        
        btn.MouseButton1Click:Connect(function()
            DoUnlock()
            Tween(btn, "BackgroundColor3", Yellow, 0.1)
            task.delay(0.15, function() Tween(btn, "BackgroundColor3", Dark, 0.1) end)
        end)
    end
    
    UnlockPanel = sg
end

local function CreateActionsUI()
    if ActionsPanel then ActionsPanel:Destroy() end
    
    local sg = Instance.new("ScreenGui")
    sg.Name = "LawActions"
    sg.ResetOnSpawn = false
    sg.Parent = LP.PlayerGui
    
    local f = Instance.new("Frame")
    f.Size = UDim2.new(0, 220, 0, 260)
    f.Position = UDim2.new(0.5, -110, 0.35, 0)
    f.BackgroundColor3 = Black
    f.BackgroundTransparency = 0.1
    f.BorderSizePixel = 0
    f.Active = true
    f.Draggable = true
    f.Parent = sg
    Corner(f, 12)
    Stroke(f, Yellow, 1.5)
    
    local t = Instance.new("TextLabel", f)
    t.Size = UDim2.new(1, 0, 0, 35)
    t.BackgroundTransparency = 1
    t.Text = "ACTIONS"
    t.Font = Enum.Font.GothamBold
    t.TextSize = 14
    t.TextColor3 = Yellow
    
    local c = Instance.new("Frame", f)
    c.Size = UDim2.new(1, -16, 1, -45)
    c.Position = UDim2.new(0, 8, 0, 40)
    c.BackgroundTransparency = 1
    
    local layout = Instance.new("UIListLayout", c)
    layout.Padding = UDim.new(0, 8)
    
    local btns = {
        {"REJOIN", Rejoin},
        {"KICK", Kick},
        {"BALLOON", Balloon},
        {"RAGDOLL", Ragdoll},
        {"RESET", Reset},
    }
    
    for _, b in ipairs(btns) do
        local btn = Instance.new("TextButton", c)
        btn.Size = UDim2.new(1, 0, 0, 42)
        btn.BackgroundColor3 = Dark
        btn.Text = b[1]
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 13
        btn.TextColor3 = Color3.fromRGB(255,255,255)
        Corner(btn, 8)
        Stroke(btn, Yellow, 1)
        
        btn.MouseEnter:Connect(function()
            Tween(btn, "BackgroundColor3", Color3.fromRGB(50,50,50), 0.1)
        end)
        btn.MouseLeave:Connect(function()
            Tween(btn, "BackgroundColor3", Dark, 0.1)
        end)
        btn.MouseButton1Click:Connect(function()
            Tween(btn, "BackgroundColor3", Yellow, 0.08)
            task.delay(0.15, function() Tween(btn, "BackgroundColor3", Dark, 0.1) end)
            pcall(b[2])
        end)
    end
    
    ActionsPanel = sg
end

-- =============================================
-- MAIN MENU (COM DRAG FUNCIONANDO)
-- =============================================
local MainFrame = nil
local LawBtn = nil
local MenuOpen = true

local function CreateMainMenu()
    if MainFrame then MainFrame:Destroy() end
    
    local sg = Instance.new("ScreenGui")
    sg.Name = "LawHub"
    sg.ResetOnSpawn = false
    sg.Parent = LP.PlayerGui
    
    local f = Instance.new("Frame")
    f.Size = UDim2.new(0, 280, 0, 380)
    f.Position = UDim2.new(0.5, -140, 0.35, 0)
    f.BackgroundColor3 = Black
    f.BackgroundTransparency = 0.05
    f.BorderSizePixel = 0
    f.Active = true
    f.Parent = sg
    Corner(f, 12)
    Stroke(f, Yellow, 2)
    
    -- BARRA DE TÍTULO (DRAG AQUI)
    local titleBar = Instance.new("Frame", f)
    titleBar.Size = UDim2.new(1, 0, 0, 45)
    titleBar.BackgroundColor3 = Dark
    titleBar.BackgroundTransparency = 0.3
    Corner(titleBar, 12)
    
    local title = Instance.new("TextLabel", titleBar)
    title.Size = UDim2.new(1, 0, 1, 0)
    title.BackgroundTransparency = 1
    title.Text = "LAW HUB"
    title.Font = Enum.Font.GothamBlack
    title.TextSize = 22
    title.TextColor3 = Yellow
    
    local line = Instance.new("Frame", f)
    line.Size = UDim2.new(0.9, 0, 0, 2)
    line.Position = UDim2.new(0.05, 0, 0, 48)
    line.BackgroundColor3 = Yellow
    
    local scroll = Instance.new("ScrollingFrame", f)
    scroll.Size = UDim2.new(1, 0, 1, -75)
    scroll.Position = UDim2.new(0, 0, 0, 55)
    scroll.BackgroundTransparency = 1
    scroll.ScrollBarThickness = 3
    scroll.ScrollBarImageColor3 = Yellow
    scroll.CanvasSize = UDim2.new(0, 0, 0, 280)
    
    local cont = Instance.new("Frame", scroll)
    cont.Size = UDim2.new(1, 0, 0, 280)
    cont.BackgroundTransparency = 1
    
    -- Switches
    local y = 5
    local switches = {
        {"INSTANT STEAL V2", "Auto teleport + steal", "instant"},
        {"ACTIVE POTION", "Potion Teleport Helper", "active"},
        {"UNLOCK BASE", "Unlock other player's base", "unlock"},
        {"ACTIONS PANEL", "Rejoin, Kick, Balloon, Ragdoll", "actions"},
    }
    
    for _, sw in ipairs(switches) do
        local row = Instance.new("Frame", cont)
        row.Size = UDim2.new(1, -20, 0, 52)
        row.Position = UDim2.new(0, 10, 0, y)
        row.BackgroundColor3 = Dark
        row.BackgroundTransparency = 0.3
        Corner(row, 8)
        
        local lbl = Instance.new("TextLabel", row)
        lbl.Size = UDim2.new(1, -70, 0, 22)
        lbl.Position = UDim2.new(0, 12, 0, 4)
        lbl.BackgroundTransparency = 1
        lbl.Text = sw[1]
        lbl.Font = Enum.Font.GothamBold
        lbl.TextSize = 13
        lbl.TextColor3 = Color3.fromRGB(255,255,255)
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        
        local dsc = Instance.new("TextLabel", row)
        dsc.Size = UDim2.new(1, -70, 0, 18)
        dsc.Position = UDim2.new(0, 12, 0, 26)
        dsc.BackgroundTransparency = 1
        dsc.Text = sw[2]
        dsc.Font = Enum.Font.Gotham
        dsc.TextSize = 10
        dsc.TextColor3 = Color3.fromRGB(150,150,150)
        dsc.TextXAlignment = Enum.TextXAlignment.Left
        
        local swBg = Instance.new("Frame", row)
        swBg.Size = UDim2.new(0, 44, 0, 24)
        swBg.Position = UDim2.new(1, -56, 0.5, -12)
        swBg.BackgroundColor3 = Color3.fromRGB(50,50,50)
        Corner(swBg, 12)
        
        local knob = Instance.new("Frame", swBg)
        knob.Size = UDim2.new(0, 20, 0, 20)
        knob.Position = UDim2.new(0, 2, 0.5, -10)
        knob.BackgroundColor3 = Color3.fromRGB(150,150,150)
        Corner(knob, 10)
        
        local click = Instance.new("TextButton", swBg)
        click.Size = UDim2.new(1, 0, 1, 0)
        click.BackgroundTransparency = 1
        click.Text = ""
        
        local id = sw[3]
        local state = false
        
        click.MouseButton1Click:Connect(function()
            state = not state
            if state then
                swBg.BackgroundColor3 = Yellow
                knob.Position = UDim2.new(1, -22, 0.5, -10)
                knob.BackgroundColor3 = Color3.fromRGB(255,255,255)
            else
                swBg.BackgroundColor3 = Color3.fromRGB(50,50,50)
                knob.Position = UDim2.new(0, 2, 0.5, -10)
                knob.BackgroundColor3 = Color3.fromRGB(150,150,150)
            end
            
            if id == "instant" then
                InstantStealOn = state
                if state then
                    CreateStealPanel()
                    StartSteal()
                    if StealSpeedOn then StartSpeed() end
                else
                    if StealPanel then StealPanel:Destroy(); StealPanel = nil end
                    StopSteal()
                    StopSpeed()
                end
            elseif id == "unlock" then
                UnlockBaseOn = state
                if state then CreateUnlockUI() else if UnlockPanel then UnlockPanel:Destroy(); UnlockPanel = nil end end
            elseif id == "actions" then
                ActionsOn = state
                if state then CreateActionsUI() else if ActionsPanel then ActionsPanel:Destroy(); ActionsPanel = nil end end
            end
        end)
        
        row.MouseEnter:Connect(function() Tween(row, "BackgroundTransparency", 0, 0.1) end)
        row.MouseLeave:Connect(function() Tween(row, "BackgroundTransparency", 0.3, 0.1) end)
        
        y = y + 60
    end
    
    local footer = Instance.new("TextLabel", f)
    footer.Size = UDim2.new(1, 0, 0, 22)
    footer.Position = UDim2.new(0, 0, 1, -24)
    footer.BackgroundTransparency = 1
    footer.Text = "LAW HUB"
    footer.Font = Enum.Font.Gotham
    footer.TextSize = 9
    footer.TextColor3 = Color3.fromRGB(100,100,100)
    
    -- DRAG (FUNCIONANDO)
    local drag = false
    local startDrag = nil
    local startFrame = nil
    
    titleBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            drag = true
            startDrag = input.Position
            startFrame = f.Position
        end
    end)
    
    titleBar.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            drag = false
        end
    end)
    
    UIS.InputChanged:Connect(function(input)
        if drag and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - startDrag
            f.Position = UDim2.new(
                startFrame.X.Scale,
                startFrame.X.Offset + delta.X,
                startFrame.Y.Scale,
                startFrame.Y.Offset + delta.Y
            )
        end
    end)
    
    MainFrame = sg
end

-- =============================================
-- BOTÃO LAW
-- =============================================
local function CreateLawButton()
    if LawBtn then LawBtn:Destroy() end
    
    local btn = Instance.new("TextButton", LP.PlayerGui)
    btn.Size = UDim2.new(0, 70, 0, 32)
    btn.Position = UDim2.new(1, -80, 0, 10)
    btn.BackgroundColor3 = Black
    btn.BackgroundTransparency = 0.2
    btn.Text = "LAW"
    btn.TextColor3 = Yellow
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 16
    Corner(btn, 8)
    Stroke(btn, Yellow, 2)
    
    btn.MouseButton1Click:Connect(function()
        MenuOpen = not MenuOpen
        if MainFrame then MainFrame.Visible = MenuOpen end
    end)
    
    LawBtn = btn
end

-- =============================================
-- INICIAR
-- =============================================
CreateLawButton()
CreateMainMenu()

print("✅ LAW HUB CARREGADO!")
print("📌 Clique no botão LAW para abrir/fechar")
print("🖱️ ARRASTE o menu pela barra superior (LAW HUB)")
