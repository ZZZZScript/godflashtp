-- =============================================
-- OLD HUB - COMPLETE SYSTEM
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local ProximityPromptService = game:GetService("ProximityPromptService")
local StarterGui = game:GetService("StarterGui")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")
local TextChatService = game:GetService("TextChatService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- =============================================
-- ANTI-CHEAT SYSTEM
-- =============================================

local function AntiKickTotal()
    pcall(function()
        local ts = TeleportService
        local tsMt = getrawmetatable(ts)
        if tsMt then
            local oldIndex = tsMt.__index
            tsMt.__index = function(self, key)
                if key == "Kick" then return function() end end
                return oldIndex(self, key)
            end
        end
        player.Kick = function() end
        Players.Kick = function() end
    end)
    
    for _, remote in ipairs(game:GetDescendants()) do
        if remote:IsA("RemoteEvent") then
            local name = string.lower(remote.Name)
            if name:find("kick") or name:find("ban") or name:find("cheat") then
                pcall(function() remote.OnServerEvent:Connect(function() return end) end)
            end
        end
    end
end

local function AntiRagdoll()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if not hum then return end
    local state = hum:GetState()
    if state == Enum.HumanoidStateType.Physics or 
       state == Enum.HumanoidStateType.Ragdoll or 
       state == Enum.HumanoidStateType.FallingDown then
        hum:ChangeState(Enum.HumanoidStateType.Running)
        for _, obj in pairs(char:GetDescendants()) do
            if obj:IsA("BallSocketConstraint") or obj:IsA("HingeConstraint") then
                obj:Destroy()
            end
        end
    end
end

local lastPos = nil
local freezeCount = 0
local function AntiFreeze()
    local char = player.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hrp and hum then
        if lastPos then
            if (hrp.Position - lastPos).Magnitude < 0.5 then
                freezeCount = freezeCount + 1
                if freezeCount > 10 then
                    hrp.AssemblyLinearVelocity = Vector3.zero
                    hum:ChangeState(Enum.HumanoidStateType.Running)
                    freezeCount = 0
                end
            else
                freezeCount = 0
            end
        end
        lastPos = hrp.Position
    end
end

local function AntiAnimationLock()
    local char = player.Character
    if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum and hum:FindFirstChild("Animator") then
        for _, track in pairs(hum.Animator:GetPlayingAnimationTracks()) do
            local name = string.lower(track.Animation and track.Animation.Name or "")
            if name:find("rag") or name:find("fall") or name:find("hurt") then
                track:Stop(0)
            end
        end
    end
end

RunService.Heartbeat:Connect(AntiRagdoll)
RunService.Heartbeat:Connect(AntiFreeze)
RunService.Heartbeat:Connect(AntiAnimationLock)
AntiKickTotal()

player.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    local hum = char:FindFirstChildOfClass("Humanoid")
    if hum then hum.BreakJointsOnDeath = false end
end)

print("ANTI-CHEAT ACTIVATED")

-- =============================================
-- TELEPORT COORDINATES
-- =============================================

-- TELEPORT 1 TO 5
local Teleport1Pos = Vector3.new(-335.1, -7.1, 39.7)
local Teleport2Pos = Vector3.new(-324.3, -7.1, 37.5)
local Teleport3Pos = Vector3.new(-316.1, -7.1, 36.7)
local Teleport4Pos = Vector3.new(-309.8, -7.1, 38.3)
local Teleport5Pos = Vector3.new(-302.3, -7.1, 41.5)

-- TELEPORT 6 TO 10 (apenas destino final)
local Teleport6Pos = Vector3.new(-331.6, -7.1, -23.3)
local Teleport7Pos = Vector3.new(-324.1, -7.1, -23.2)
local Teleport8Pos = Vector3.new(-317.8, -7.1, -23.6)
local Teleport9Pos = Vector3.new(-311.1, -7.1, -23.5)
local Teleport10Pos = Vector3.new(-302.6, -7.1, -23.9)

-- =============================================
-- VARIABLES
-- =============================================
local flashEnabled = true
local selectedTeleport = nil
local teleporting = false
local autoBlockEnabled = false
local dropdownOpen = false

-- =============================================
-- FUNCTIONS
-- =============================================
local function getHRP()
    local char = player.Character
    return char and char:FindFirstChild("HumanoidRootPart")
end

local function getHum()
    local char = player.Character
    return char and char:FindFirstChildOfClass("Humanoid")
end

local function EquipCarpet()
    local char = player.Character
    local bp = player:FindFirstChild("Backpack")
    if not char or not bp then return end
    
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") then
            tool.Parent = bp
        end
    end
    
    local carpet = bp:FindFirstChild("Flying Carpet")
    local hum = getHum()
    if carpet and hum then
        hum:EquipTool(carpet)
        task.wait(0.1)
    end
end

local function UseFlash()
    local char = player.Character
    local bp = player:FindFirstChild("Backpack")
    if not char then return end
    
    local flash = nil
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") and (tool.Name:lower():find("flash") or tool.Name:lower():find("flashlight")) then
            flash = tool
            break
        end
    end
    
    if not flash and bp then
        for _, tool in pairs(bp:GetChildren()) do
            if tool:IsA("Tool") and (tool.Name:lower():find("flash") or tool.Name:lower():find("flashlight")) then
                flash = tool
                break
            end
        end
    end
    
    if flash then
        if flash.Parent == bp then
            local hum = getHum()
            if hum then hum:EquipTool(flash) end
            task.wait(0.1)
        end
        pcall(function() flash:Activate() end)
        task.wait(0.15)
    end
end

-- Teleport com controle de camera
-- lookDir: "front" = frente, "left" = esquerda, "right" = direita, "back" = tras
local function TeleportTo(pos, cameraDirection)
    local hrp = getHRP()
    if not hrp then return false end
    
    hrp.CFrame = CFrame.new(pos)
    hrp.AssemblyLinearVelocity = Vector3.zero
    task.wait(0.05)
    
    local cam = workspace.CurrentCamera
    if cam and hrp then
        local lookDir
        if cameraDirection == "left" then
            lookDir = hrp.CFrame.RightVector
        elseif cameraDirection == "right" then
            lookDir = -hrp.CFrame.RightVector
        elseif cameraDirection == "back" then
            lookDir = -hrp.CFrame.LookVector
        else
            lookDir = hrp.CFrame.LookVector
        end
        cam.CFrame = CFrame.new(hrp.Position + Vector3.new(0, 2, 0), hrp.Position + lookDir + Vector3.new(0, 2, 0))
    end
    
    return true
end

local function ExecuteTeleport()
    if not selectedTeleport then
        StarterGui:SetCore("SendNotification", {Title = "Warning", Text = "Select a teleport first!", Duration = 2})
        return
    end
    
    if teleporting then return end
    teleporting = true
    
    EquipCarpet()
    task.wait(0.1)
    
    if flashEnabled then
        UseFlash()
    end
    
    -- TELEPORT 1 TO 5 (camera normal)
    if selectedTeleport == 1 then
        TeleportTo(Teleport1Pos, "front")
    elseif selectedTeleport == 2 then
        TeleportTo(Teleport2Pos, "front")
    elseif selectedTeleport == 3 then
        TeleportTo(Teleport3Pos, "front")
    elseif selectedTeleport == 4 then
        TeleportTo(Teleport4Pos, "front")
    elseif selectedTeleport == 5 then
        TeleportTo(Teleport5Pos, "front")
        
    -- TELEPORT 6 TO 10 (camera para ESQUERDA)
    elseif selectedTeleport == 6 then
        TeleportTo(Teleport6Pos, "left")
    elseif selectedTeleport == 7 then
        TeleportTo(Teleport7Pos, "left")
    elseif selectedTeleport == 8 then
        TeleportTo(Teleport8Pos, "left")
    elseif selectedTeleport == 9 then
        TeleportTo(Teleport9Pos, "left")
    elseif selectedTeleport == 10 then
        TeleportTo(Teleport10Pos, "left")
    end
    
    teleporting = false
    StarterGui:SetCore("SendNotification", {Title = "Complete", Text = "TELEPORT " .. selectedTeleport, Duration = 2})
end

-- =============================================
-- INSTANT RESET (MAIS RAPIDO)
-- =============================================
local function InstaReset()
    local char = player.Character
    if not char then 
        player:LoadCharacter()
        return
    end
    
    local hum = char:FindFirstChild("Humanoid")
    local hrp = char:FindFirstChild("HumanoidRootPart")
    
    if hrp then
        hrp.CFrame = CFrame.new(hrp.Position.X, -1000, hrp.Position.Z)
        hrp.AssemblyLinearVelocity = Vector3.zero
    end
    
    if hum then
        hum.Health = 0
        hum:BreakJoints()
    end
    
    -- Sem delay, respawn imediato
    player:LoadCharacter()
    
    print("INSTANT RESET EXECUTED!")
end

local function getNearestPlayer()
    local hrp = getHRP()
    if not hrp then return nil end
    
    local nearest = nil
    local shortest = math.huge
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player then
            local otherHrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if otherHrp then
                local dist = (hrp.Position - otherHrp.Position).Magnitude
                if dist < shortest then
                    shortest = dist
                    nearest = plr
                end
            end
        end
    end
    return nearest
end

local function blockPlayer(plr)
    if not plr or plr == player then return end
    pcall(function() StarterGui:SetCore("PromptBlockPlayer", plr) end)
end

local function OnFPressed()
    ExecuteTeleport()
    task.wait(0.3)
    if autoBlockEnabled then
        local nearest = getNearestPlayer()
        if nearest then
            blockPlayer(nearest)
        end
    end
end

-- =============================================
-- GUI
-- =============================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "OldHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = playerGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = 999

-- Progress Bar
local StealBarBG = Instance.new("Frame")
StealBarBG.Size = UDim2.new(0, 220, 0, 16)
StealBarBG.Position = UDim2.new(0.5, -110, 1, -24)
StealBarBG.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
StealBarBG.BackgroundTransparency = 0
StealBarBG.BorderSizePixel = 0
StealBarBG.Parent = ScreenGui

local StealBarBGCorner = Instance.new("UICorner")
StealBarBGCorner.CornerRadius = UDim.new(0, 6)
StealBarBGCorner.Parent = StealBarBG

local StealBarStroke = Instance.new("UIStroke")
StealBarStroke.Color = Color3.fromRGB(180, 180, 180)
StealBarStroke.Thickness = 1.5
StealBarStroke.Parent = StealBarBG

local StealBarInner = Instance.new("Frame")
StealBarInner.Size = UDim2.new(1, -8, 1, -6)
StealBarInner.Position = UDim2.new(0, 4, 0, 3)
StealBarInner.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
StealBarInner.BorderSizePixel = 0
StealBarInner.Parent = StealBarBG

local StealBarInnerCorner = Instance.new("UICorner")
StealBarInnerCorner.CornerRadius = UDim.new(1, 0)
StealBarInnerCorner.Parent = StealBarInner

local StealBarFill = Instance.new("Frame")
StealBarFill.Size = UDim2.new(0, 0, 1, 0)
StealBarFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
StealBarFill.BorderSizePixel = 0
StealBarFill.Parent = StealBarInner

-- Main Frame
local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 260, 0, 280)
Main.Position = UDim2.new(0.5, -130, 0.5, -140)
Main.BackgroundColor3 = Color3.new(0, 0, 0)
Main.BackgroundTransparency = 0.2
Main.BorderSizePixel = 0
Main.Parent = ScreenGui
Main.Active = true

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.new(1, 1, 1)
MainStroke.Thickness = 1.5
MainStroke.Parent = Main

-- Title Bar
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 35)
TitleBar.BackgroundColor3 = Color3.new(0, 0, 0)
TitleBar.BackgroundTransparency = 0.5
TitleBar.BorderSizePixel = 0
TitleBar.Parent = Main

local TitleBarCorner = Instance.new("UICorner")
TitleBarCorner.CornerRadius = UDim.new(0, 12)
TitleBarCorner.Parent = TitleBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 1, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "OLD HUB"
TitleLabel.TextColor3 = Color3.new(1, 1, 1)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 16
TitleLabel.TextXAlignment = Enum.TextXAlignment.Center
TitleLabel.Parent = TitleBar

-- DRAG SYSTEM
local dragging = false
local dragStart, startPos

TitleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = Main.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.TouchMoved:Connect(function(input)
    if dragging then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

-- Content
local Content = Instance.new("Frame")
Content.Size = UDim2.new(1, -16, 1, -45)
Content.Position = UDim2.new(0, 8, 0, 40)
Content.BackgroundTransparency = 1
Content.Parent = Main
Content.ZIndex = 10

-- DROPDOWN BUTTON
local DropdownButton = Instance.new("TextButton")
DropdownButton.Size = UDim2.new(1, 0, 0, 38)
DropdownButton.Position = UDim2.new(0, 0, 0, 0)
DropdownButton.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
DropdownButton.Text = ""
DropdownButton.BorderSizePixel = 0
DropdownButton.Parent = Content
DropdownButton.ZIndex = 20

local DropdownCorner = Instance.new("UICorner")
DropdownCorner.CornerRadius = UDim.new(0, 8)
DropdownCorner.Parent = DropdownButton

local DropdownLabel = Instance.new("TextLabel")
DropdownLabel.Size = UDim2.new(1, -20, 1, 0)
DropdownLabel.Position = UDim2.new(0, 12, 0, 0)
DropdownLabel.BackgroundTransparency = 1
DropdownLabel.Text = "SELECT TELEPORT"
DropdownLabel.TextColor3 = Color3.new(1, 1, 1)
DropdownLabel.Font = Enum.Font.GothamBold
DropdownLabel.TextSize = 12
DropdownLabel.TextXAlignment = Enum.TextXAlignment.Left
DropdownLabel.Parent = DropdownButton
DropdownLabel.ZIndex = 20

-- DROPDOWN OPTIONS
local OptionsFrame = Instance.new("Frame")
OptionsFrame.Size = UDim2.new(1, 0, 0, 260)
OptionsFrame.Position = UDim2.new(0, 0, 0, 40)
OptionsFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
OptionsFrame.BackgroundTransparency = 0.1
OptionsFrame.Visible = false
OptionsFrame.BorderSizePixel = 0
OptionsFrame.Parent = Content
OptionsFrame.ZIndex = 25

local OptionsCorner = Instance.new("UICorner")
OptionsCorner.CornerRadius = UDim.new(0, 8)
OptionsCorner.Parent = OptionsFrame

local OptionsStroke = Instance.new("UIStroke")
OptionsStroke.Color = Color3.new(1, 1, 1)
OptionsStroke.Thickness = 1
OptionsStroke.Parent = OptionsFrame

local OptionsScroll = Instance.new("ScrollingFrame")
OptionsScroll.Size = UDim2.new(1, -4, 1, -4)
OptionsScroll.Position = UDim2.new(0, 2, 0, 2)
OptionsScroll.BackgroundTransparency = 1
OptionsScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
OptionsScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
OptionsScroll.ScrollBarThickness = 4
OptionsScroll.Parent = OptionsFrame
OptionsScroll.ZIndex = 25

local ScrollLayout = Instance.new("UIListLayout")
ScrollLayout.Padding = UDim.new(0, 2)
ScrollLayout.Parent = OptionsScroll

local teleportNames = {"1. TELEPORT 1", "2. TELEPORT 2", "3. TELEPORT 3", "4. TELEPORT 4", "5. TELEPORT 5", "6. TELEPORT 6", "7. TELEPORT 7", "8. TELEPORT 8", "9. TELEPORT 9", "10. TELEPORT 10"}

for i = 1, 10 do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, 0, 0, 34)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    btn.Text = teleportNames[i]
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 12
    btn.BorderSizePixel = 0
    btn.Parent = OptionsScroll
    btn.ZIndex = 25
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 5)
    btnCorner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        selectedTeleport = i
        DropdownLabel.Text = "SELECT TELEPORT: " .. teleportNames[i]
        OptionsFrame.Visible = false
        dropdownOpen = false
        Main.Size = UDim2.new(0, 260, 0, 280)
    end)
end

-- Toggle dropdown
DropdownButton.MouseButton1Click:Connect(function()
    dropdownOpen = not dropdownOpen
    OptionsFrame.Visible = dropdownOpen
    if dropdownOpen then
        Main.Size = UDim2.new(0, 260, 0, 360)
    else
        Main.Size = UDim2.new(0, 260, 0, 280)
    end
end)

-- Close when clicking outside
UserInputService.InputBegan:Connect(function(input)
    if dropdownOpen and input.UserInputType == Enum.UserInputType.MouseButton1 then
        local mousePos = UserInputService:GetMouseLocation()
        local btnPos = DropdownButton.AbsolutePosition
        local btnSize = DropdownButton.AbsoluteSize
        local optionsPos = OptionsFrame.AbsolutePosition
        local optionsSize = OptionsFrame.AbsoluteSize
        
        local inside = (mousePos.X >= btnPos.X and mousePos.X <= btnPos.X + btnSize.X and
                        mousePos.Y >= btnPos.Y and mousePos.Y <= btnPos.Y + btnSize.Y) or
                       (mousePos.X >= optionsPos.X and mousePos.X <= optionsPos.X + optionsSize.X and
                        mousePos.Y >= optionsPos.Y and mousePos.Y <= optionsPos.Y + optionsSize.Y)
        
        if not inside then
            OptionsFrame.Visible = false
            dropdownOpen = false
            Main.Size = UDim2.new(0, 260, 0, 280)
        end
    end
end)

-- EXECUTE BUTTON (F)
local ExecuteButton = Instance.new("TextButton")
ExecuteButton.Size = UDim2.new(1, 0, 0, 40)
ExecuteButton.Position = UDim2.new(0, 0, 0, 46)
ExecuteButton.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
ExecuteButton.Text = ""
ExecuteButton.BorderSizePixel = 0
ExecuteButton.Parent = Content
ExecuteButton.ZIndex = 15

local ExecuteCorner = Instance.new("UICorner")
ExecuteCorner.CornerRadius = UDim.new(0, 8)
ExecuteCorner.Parent = ExecuteButton

local ExecuteLabel = Instance.new("TextLabel")
ExecuteLabel.Size = UDim2.new(1, -20, 1, 0)
ExecuteLabel.Position = UDim2.new(0, 12, 0, 0)
ExecuteLabel.BackgroundTransparency = 1
ExecuteLabel.Text = "EXECUTE (F)"
ExecuteLabel.TextColor3 = Color3.new(1, 1, 1)
ExecuteLabel.Font = Enum.Font.GothamBold
ExecuteLabel.TextSize = 13
ExecuteLabel.TextXAlignment = Enum.TextXAlignment.Left
ExecuteLabel.Parent = ExecuteButton

ExecuteButton.MouseButton1Click:Connect(OnFPressed)

-- FLASH TOGGLE
local FlashButton = Instance.new("TextButton")
FlashButton.Size = UDim2.new(1, 0, 0, 34)
FlashButton.Position = UDim2.new(0, 0, 0, 92)
FlashButton.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
FlashButton.Text = ""
FlashButton.BorderSizePixel = 0
FlashButton.Parent = Content
FlashButton.ZIndex = 15

local FlashCorner = Instance.new("UICorner")
FlashCorner.CornerRadius = UDim.new(0, 8)
FlashCorner.Parent = FlashButton

local FlashLabel = Instance.new("TextLabel")
FlashLabel.Size = UDim2.new(1, -20, 1, 0)
FlashLabel.Position = UDim2.new(0, 12, 0, 0)
FlashLabel.BackgroundTransparency = 1
FlashLabel.Text = "FLASH: ON"
FlashLabel.TextColor3 = Color3.new(1, 1, 1)
FlashLabel.Font = Enum.Font.GothamBold
FlashLabel.TextSize = 12
FlashLabel.TextXAlignment = Enum.TextXAlignment.Left
FlashLabel.Parent = FlashButton

FlashButton.MouseButton1Click:Connect(function()
    flashEnabled = not flashEnabled
    FlashLabel.Text = flashEnabled and "FLASH: ON" or "FLASH: OFF"
end)

-- PREBLOCK TOGGLE
local BlockButton = Instance.new("TextButton")
BlockButton.Size = UDim2.new(1, 0, 0, 34)
BlockButton.Position = UDim2.new(0, 0, 0, 132)
BlockButton.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
BlockButton.Text = ""
BlockButton.BorderSizePixel = 0
BlockButton.Parent = Content
BlockButton.ZIndex = 15

local BlockCorner = Instance.new("UICorner")
BlockCorner.CornerRadius = UDim.new(0, 8)
BlockCorner.Parent = BlockButton

local BlockLabel = Instance.new("TextLabel")
BlockLabel.Size = UDim2.new(1, -20, 1, 0)
BlockLabel.Position = UDim2.new(0, 12, 0, 0)
BlockLabel.BackgroundTransparency = 1
BlockLabel.Text = "PREBLOCK: OFF"
BlockLabel.TextColor3 = Color3.new(1, 1, 1)
BlockLabel.Font = Enum.Font.GothamBold
BlockLabel.TextSize = 12
BlockLabel.TextXAlignment = Enum.TextXAlignment.Left
BlockLabel.Parent = BlockButton

BlockButton.MouseButton1Click:Connect(function()
    autoBlockEnabled = not autoBlockEnabled
    BlockLabel.Text = autoBlockEnabled and "PREBLOCK: ON" or "PREBLOCK: OFF"
end)

-- RESET BUTTON (R)
local ResetButton = Instance.new("TextButton")
ResetButton.Size = UDim2.new(1, 0, 0, 40)
ResetButton.Position = UDim2.new(0, 0, 0, 172)
ResetButton.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
ResetButton.Text = ""
ResetButton.BorderSizePixel = 0
ResetButton.Parent = Content
ResetButton.ZIndex = 15

local ResetCorner = Instance.new("UICorner")
ResetCorner.CornerRadius = UDim.new(0, 8)
ResetCorner.Parent = ResetButton

local ResetLabel = Instance.new("TextLabel")
ResetLabel.Size = UDim2.new(1, -20, 1, 0)
ResetLabel.Position = UDim2.new(0, 12, 0, 0)
ResetLabel.BackgroundTransparency = 1
ResetLabel.Text = "INSTANT RESET (R)"
ResetLabel.TextColor3 = Color3.new(1, 1, 1)
ResetLabel.Font = Enum.Font.GothamBold
ResetLabel.TextSize = 13
ResetLabel.TextXAlignment = Enum.TextXAlignment.Left
ResetLabel.Parent = ResetButton

ResetButton.MouseButton1Click:Connect(InstaReset)
ResetButton.TouchTap:Connect(InstaReset)

-- =============================================
-- FLASH STEAL
-- =============================================
ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt)
    if not flashEnabled then return end
    
    local start = os.clock()
    local fired = false
    
    StealBarFill.BackgroundTransparency = 0
    StealBarBG.BackgroundTransparency = 0
    StealBarFill.Size = UDim2.new(0, 0, 1, 0)
    
    local conn = RunService.PreRender:Connect(function()
        if not prompt or not prompt.Parent then
            conn:Disconnect()
            TweenService:Create(StealBarFill, TweenInfo.new(0.3), {Size = UDim2.new(0, 0, 1, 0)}):Play()
            return
        end
        
        local progress = math.clamp((os.clock() - start) / prompt.HoldDuration, 0, 1)
        StealBarFill.Size = UDim2.new(progress, 0, 1, 0)
        
        if not fired and progress >= 0.95 then
            fired = true
            conn:Disconnect()
            local char = player.Character
            local tool = char and char:FindFirstChildOfClass("Tool")
            if tool then tool:Activate() end
            
            StealBarFill.Size = UDim2.new(1, 0, 1, 0)
            task.delay(0.2, function()
                TweenService:Create(StealBarFill, TweenInfo.new(0.4), {Size = UDim2.new(0, 0, 1, 0)}):Play()
            end)
        end
    end)
    
    prompt.PromptButtonHoldEnded:Connect(function()
        if not fired then
            conn:Disconnect()
            TweenService:Create(StealBarFill, TweenInfo.new(0.3), {Size = UDim2.new(0, 0, 1, 0)}):Play()
        end
    end)
end)

-- =============================================
-- HOTKEYS
-- =============================================
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
    
    if input.KeyCode == Enum.KeyCode.F then
        OnFPressed()
    elseif input.KeyCode == Enum.KeyCode.R then
        InstaReset()
    end
end)

print("========================================")
print("OLD HUB LOADED!")
print("TELEPORT 1-5: Camera FRONT")
print("TELEPORT 6-10: Camera LEFT (olhando para os botoes)")
print("F = Execute Teleport")
print("R = Instant Reset")
print("========================================")
