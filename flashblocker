-- =============================================
-- LAW HUB - FLASH STEAL + TELEPORT (CatHUB Style)
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local ProximityPromptService = game:GetService("ProximityPromptService")
local StarterGui = game:GetService("StarterGui")
local VirtualInputManager = game:GetService("VirtualInputManager")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- =============================================
-- TELEPORT POINTS (1 TO 10)
-- =============================================
local StartPoint = Vector3.new(-357.6, -7.1, 37.4) -- Only for Teleport 6-10

local TeleportPoints = {
    -- PONTO 1 (3 teleports juntos)
    Vector3.new(-368, -7, 69),
    Vector3.new(-336, -5, 100),
    Vector3.new(-352, -7, 76),
    -- PONTO 2 ao 5
    Vector3.new(-324.3, -7.1, 37.5),
    Vector3.new(-316.1, -7.1, 36.7),
    Vector3.new(-309.8, -7.1, 38.3),
    Vector3.new(-302.3, -7.1, 41.5),
    -- PONTO 6 ao 10 (com start point)
    Vector3.new(-331.6, -7.1, -23.3),
    Vector3.new(-324.1, -7.1, -23.2),
    Vector3.new(-317.8, -7.1, -23.6),
    Vector3.new(-311.1, -7.1, -23.5),
    Vector3.new(-302.6, -7.1, -23.9),
}

-- =============================================
-- VARIABLES
-- =============================================
local flashEnabled = true
local sliderValue = 1
local manualOverride = false
local triggerBump = false
local activeTriggers = {}
local autoResetEnabled = false
local autoBlockEnabled = false
local teleportIndex = 1
local teleporting = false

-- =============================================
-- FUNCTIONS
-- =============================================
local function getHRP()
    local char = player.Character
    return char and char:FindFirstChild("HumanoidRootPart")
end

local function TeleportTo(pos)
    local hrp = getHRP()
    if hrp then
        hrp.CFrame = CFrame.new(pos)
        hrp.AssemblyLinearVelocity = Vector3.zero
    end
end

local function EquipCarpet()
    local char = player.Character
    local bp = player:FindFirstChild("Backpack")
    if not char or not bp then return end
    
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") then
            tool.Parent = bp
        end
    end
    
    local carpet = bp:FindFirstChild("Flying Carpet")
    local hum = char:FindFirstChildOfClass("Humanoid")
    if carpet and hum then
        hum:EquipTool(carpet)
        task.wait(0.2)
    end
end

local function UseFlash()
    local char = player.Character
    local bp = player:FindFirstChild("Backpack")
    if not char then return end
    
    local flash = nil
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") and (tool.Name:lower():find("flash") or tool.Name:lower():find("flashlight")) then
            flash = tool
            break
        end
    end
    
    if not flash and bp then
        for _, tool in pairs(bp:GetChildren()) do
            if tool:IsA("Tool") and (tool.Name:lower():find("flash") or tool.Name:lower():find("flashlight")) then
                flash = tool
                break
            end
        end
    end
    
    if flash then
        if flash.Parent == bp then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then hum:EquipTool(flash) end
            task.wait(0.1)
        end
        pcall(function() flash:Activate() end)
        
        for _, child in ipairs(flash:GetDescendants()) do
            if child:IsA("RemoteEvent") then
                pcall(function() child:FireServer() end)
            elseif child:IsA("RemoteFunction") then
                pcall(function() child:InvokeServer() end)
            end
        end
    end
end

-- =============================================
-- EXECUTE SEQUENTIAL TELEPORT (1 TO 10)
-- =============================================
local function ExecuteSequentialTeleport()
    if teleporting then
        print("Already teleporting...")
        return
    end
    
    teleporting = true
    print("========== STARTING SEQUENTIAL TELEPORT (1 TO 10) ==========")
    
    -- Equip Carpet and Flash
    EquipCarpet()
    task.wait(0.2)
    UseFlash()
    task.wait(0.1)
    
    -- Teleport through all points
    for i, point in ipairs(TeleportPoints) do
        -- For points 8-12 (Teleport 6-10), go to start point first
        if i == 8 then -- Before Teleport 6
            print("📍 Going to start point...")
            TeleportTo(StartPoint)
            task.wait(0.15)
        end
        
        TeleportTo(point)
        print("📍 Teleport " .. i .. "/" .. #TeleportPoints)
        task.wait(0.15)
    end
    
    print("✅ SEQUENTIAL TELEPORT COMPLETED!")
    teleporting = false
    
    StarterGui:SetCore("SendNotification", {
        Title = "Complete",
        Text = "Teleport 1 to 10 executed!",
        Duration = 2
    })
end

-- =============================================
-- RESET FUNCTION (Normal reset, no out of map)
-- =============================================
local function ExecuteReset()
    local char = player.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")
    
    if hrp and hum then
        hum.Health = 0
        print("🔄 Reset executed!")
        
        StarterGui:SetCore("SendNotification", {
            Title = "Reset",
            Text = "Character reset!",
            Duration = 1
        })
    end
end

-- =============================================
-- AUTO BLOCK FUNCTIONS
-- =============================================
local function getNearestPlayer()
    local char = player.Character
    if not char then return nil end
    local rootPart = char:FindFirstChild("HumanoidRootPart")
    if not rootPart then return nil end
    
    local nearestPlayer = nil
    local shortestDistance = math.huge
    
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player then
            local otherChar = plr.Character
            if otherChar then
                local otherRoot = otherChar:FindFirstChild("HumanoidRootPart")
                if otherRoot then
                    local distance = (rootPart.Position - otherRoot.Position).Magnitude
                    if distance < shortestDistance then
                        shortestDistance = distance
                        nearestPlayer = plr
                    end
                end
            end
        end
    end
    return nearestPlayer
end

local function blockPlayer(plr)
    if not plr or plr == player then return end
    pcall(function()
        StarterGui:SetCore("PromptBlockPlayer", plr)
    end)
end

local function autoBlockClick()
    VirtualInputManager:SendMouseButtonEvent(963, 516, 0, true, game, 1)
    task.wait(0.05)
    VirtualInputManager:SendMouseButtonEvent(963, 516, 0, false, game, 1)
end

-- =============================================
-- CREATE GUI (CatHUB STYLE)
-- =============================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LawHub_Flash"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = playerGui

-- Steal Progress Bar
local StealBarBG = Instance.new("Frame")
StealBarBG.Name = "StealBarBG"
StealBarBG.Size = UDim2.new(0, 220, 0, 16)
StealBarBG.Position = UDim2.new(0.5, -110, 1, -24)
StealBarBG.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
StealBarBG.BackgroundTransparency = 0
StealBarBG.BorderSizePixel = 0
StealBarBG.Parent = ScreenGui

local StealBarBGCorner = Instance.new("UICorner")
StealBarBGCorner.CornerRadius = UDim.new(0, 6)
StealBarBGCorner.Parent = StealBarBG

local StealBarStroke = Instance.new("UIStroke")
StealBarStroke.Color = Color3.fromRGB(180, 180, 180)
StealBarStroke.Thickness = 1.5
StealBarStroke.Parent = StealBarBG

local StealBarInner = Instance.new("Frame")
StealBarInner.Name = "Inner"
StealBarInner.Size = UDim2.new(1, -8, 1, -6)
StealBarInner.Position = UDim2.new(0, 4, 0, 3)
StealBarInner.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
StealBarInner.BorderSizePixel = 0
StealBarInner.Parent = StealBarBG

local StealBarInnerCorner = Instance.new("UICorner")
StealBarInnerCorner.CornerRadius = UDim.new(1, 0)
StealBarInnerCorner.Parent = StealBarInner

local StealBarFill = Instance.new("Frame")
StealBarFill.Name = "Fill"
StealBarFill.Size = UDim2.new(0, 0, 1, 0)
StealBarFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
StealBarFill.BorderSizePixel = 0
StealBarFill.Parent = StealBarInner

local StealFillCorner = Instance.new("UICorner")
StealFillCorner.CornerRadius = UDim.new(1, 0)
StealFillCorner.Parent = StealBarFill

-- Main Frame
local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Size = UDim2.new(0, 220, 0, 230)
Main.Position = UDim2.new(0.5, -110, 0.5, -115)
Main.BackgroundColor3 = Color3.new(0, 0, 0)
Main.BackgroundTransparency = 0.2
Main.BorderSizePixel = 0
Main.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = Main

-- Animated Stroke
local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.new(1, 1, 1)
MainStroke.Thickness = 1.3
MainStroke.Parent = Main

local StrokeGradient = Instance.new("UIGradient")
StrokeGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.new(1, 1, 1)),
    ColorSequenceKeypoint.new(0.25, Color3.new(0, 0, 0)),
    ColorSequenceKeypoint.new(0.5, Color3.new(1, 1, 1)),
    ColorSequenceKeypoint.new(0.75, Color3.new(0, 0, 0)),
    ColorSequenceKeypoint.new(1, Color3.new(1, 1, 1)),
})
StrokeGradient.Parent = MainStroke

task.spawn(function()
    while StrokeGradient and StrokeGradient.Parent do
        StrokeGradient.Rotation = (StrokeGradient.Rotation + 0.5) % 360
        task.wait()
    end
end)

-- Title Bar
local TitleBar = Instance.new("Frame")
TitleBar.Name = "TitleBar"
TitleBar.Size = UDim2.new(1, 0, 0, 32)
TitleBar.BackgroundColor3 = Color3.new(0, 0, 0)
TitleBar.BackgroundTransparency = 0.5
TitleBar.BorderSizePixel = 0
TitleBar.Parent = Main

local TitleBarCorner = Instance.new("UICorner")
TitleBarCorner.CornerRadius = UDim.new(0, 12)
TitleBarCorner.Parent = TitleBar

local TitleSep = Instance.new("Frame")
TitleSep.Name = "Separator"
TitleSep.Size = UDim2.new(1, 0, 0, 1)
TitleSep.Position = UDim2.new(0, 0, 1, 0)
TitleSep.BackgroundColor3 = Color3.new(1, 1, 1)
TitleSep.BorderSizePixel = 0
TitleSep.Parent = TitleBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "Title"
TitleLabel.Size = UDim2.new(1, -20, 1, 0)
TitleLabel.Position = UDim2.new(0, 10, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "LAW HUB"
TitleLabel.TextColor3 = Color3.new(1, 1, 1)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextSize = 14
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleBar

-- Dragging
do
    local dragging = false
    local dragInput, dragStart, startPos

    TitleBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = Main.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            Main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

-- Content Area
local Content = Instance.new("Frame")
Content.Name = "Content"
Content.Size = UDim2.new(1, -20, 1, -42)
Content.Position = UDim2.new(0, 10, 0, 37)
Content.BackgroundTransparency = 1
Content.BorderSizePixel = 0
Content.Parent = Main

-- Teleport Button (Main Action)
local TeleportButton = Instance.new("TextButton")
TeleportButton.Name = "TeleportButton"
TeleportButton.Size = UDim2.new(1, 0, 0, 40)
TeleportButton.Position = UDim2.new(0, 0, 0, 0)
TeleportButton.BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)
TeleportButton.AutoButtonColor = false
TeleportButton.Text = ""
TeleportButton.BorderSizePixel = 0
TeleportButton.Parent = Content

local TeleportButtonCorner = Instance.new("UICorner")
TeleportButtonCorner.CornerRadius = UDim.new(0, 8)
TeleportButtonCorner.Parent = TeleportButton

local TeleportLabel = Instance.new("TextLabel")
TeleportLabel.Name = "Label"
TeleportLabel.Size = UDim2.new(1, -12, 1, 0)
TeleportLabel.Position = UDim2.new(0, 12, 0, 0)
TeleportLabel.BackgroundTransparency = 1
TeleportLabel.Text = "🚀 SEQUENTIAL TP (1→10)"
TeleportLabel.TextColor3 = Color3.new(1, 1, 1)
TeleportLabel.Font = Enum.Font.GothamBold
TeleportLabel.TextSize = 12
TeleportLabel.TextXAlignment = Enum.TextXAlignment.Left
TeleportLabel.Parent = TeleportButton

TeleportButton.MouseEnter:Connect(function()
    TweenService:Create(TeleportButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.new(0.18, 0.18, 0.18)}):Play()
end)
TeleportButton.MouseLeave:Connect(function()
    TweenService:Create(TeleportButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)}):Play()
end)

TeleportButton.MouseButton1Click:Connect(function()
    ExecuteSequentialTeleport()
end)

-- Flash TP Toggle Button
local FlashButton = Instance.new("TextButton")
FlashButton.Name = "FlashButton"
FlashButton.Size = UDim2.new(1, 0, 0, 36)
FlashButton.Position = UDim2.new(0, 0, 0, 46)
FlashButton.BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)
FlashButton.AutoButtonColor = false
FlashButton.Text = ""
FlashButton.BorderSizePixel = 0
FlashButton.Parent = Content

local FlashButtonCorner = Instance.new("UICorner")
FlashButtonCorner.CornerRadius = UDim.new(0, 8)
FlashButtonCorner.Parent = FlashButton

local FlashLabel = Instance.new("TextLabel")
FlashLabel.Name = "Label"
FlashLabel.Size = UDim2.new(1, -12, 1, 0)
FlashLabel.Position = UDim2.new(0, 12, 0, 0)
FlashLabel.BackgroundTransparency = 1
FlashLabel.Text = "⚡ FLASH TP: ON"
FlashLabel.TextColor3 = Color3.new(1, 1, 1)
FlashLabel.Font = Enum.Font.GothamBold
FlashLabel.TextSize = 12
FlashLabel.TextXAlignment = Enum.TextXAlignment.Left
FlashLabel.Parent = FlashButton

FlashButton.MouseEnter:Connect(function()
    TweenService:Create(FlashButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.new(0.18, 0.18, 0.18)}):Play()
end)
FlashButton.MouseLeave:Connect(function()
    TweenService:Create(FlashButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)}):Play()
end)

FlashButton.MouseButton1Click:Connect(function()
    flashEnabled = not flashEnabled
    if flashEnabled then
        FlashLabel.Text = "⚡ FLASH TP: ON"
        FlashButton.BackgroundColor3 = Color3.new(0.2, 0.2, 0.2)
    else
        FlashLabel.Text = "⚡ FLASH TP: OFF"
        FlashButton.BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)
    end
end)

-- Auto Block Toggle Button
local BlockButton = Instance.new("TextButton")
BlockButton.Name = "BlockButton"
BlockButton.Size = UDim2.new(1, 0, 0, 36)
BlockButton.Position = UDim2.new(0, 0, 0, 88)
BlockButton.BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)
BlockButton.AutoButtonColor = false
BlockButton.Text = ""
BlockButton.BorderSizePixel = 0
BlockButton.Parent = Content

local BlockButtonCorner = Instance.new("UICorner")
BlockButtonCorner.CornerRadius = UDim.new(0, 8)
BlockButtonCorner.Parent = BlockButton

local BlockLabel = Instance.new("TextLabel")
BlockLabel.Name = "Label"
BlockLabel.Size = UDim2.new(1, -12, 1, 0)
BlockLabel.Position = UDim2.new(0, 12, 0, 0)
BlockLabel.BackgroundTransparency = 1
BlockLabel.Text = "🛡️ AUTO BLOCK: OFF"
BlockLabel.TextColor3 = Color3.new(1, 1, 1)
BlockLabel.Font = Enum.Font.GothamBold
BlockLabel.TextSize = 12
BlockLabel.TextXAlignment = Enum.TextXAlignment.Left
BlockLabel.Parent = BlockButton

BlockButton.MouseEnter:Connect(function()
    TweenService:Create(BlockButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.new(0.18, 0.18, 0.18)}):Play()
end)
BlockButton.MouseLeave:Connect(function()
    TweenService:Create(BlockButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)}):Play()
end)

BlockButton.MouseButton1Click:Connect(function()
    autoBlockEnabled = not autoBlockEnabled
    BlockLabel.Text = autoBlockEnabled and "🛡️ AUTO BLOCK: ON" or "🛡️ AUTO BLOCK: OFF"
    BlockLabel.TextColor3 = autoBlockEnabled and Color3.new(0.3, 1, 0.3) or Color3.new(1, 1, 1)
    
    if autoBlockEnabled then
        local nearest = getNearestPlayer()
        if nearest then
            blockPlayer(nearest)
            task.wait(0.1)
            autoBlockClick()
        end
    end
end)

-- Reset Button
local ResetButton = Instance.new("TextButton")
ResetButton.Name = "ResetButton"
ResetButton.Size = UDim2.new(1, 0, 0, 36)
ResetButton.Position = UDim2.new(0, 0, 0, 130)
ResetButton.BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)
ResetButton.AutoButtonColor = false
ResetButton.Text = ""
ResetButton.BorderSizePixel = 0
ResetButton.Parent = Content

local ResetButtonCorner = Instance.new("UICorner")
ResetButtonCorner.CornerRadius = UDim.new(0, 8)
ResetButtonCorner.Parent = ResetButton

local ResetLabel = Instance.new("TextLabel")
ResetLabel.Name = "Label"
ResetLabel.Size = UDim2.new(1, -12, 1, 0)
ResetLabel.Position = UDim2.new(0, 12, 0, 0)
ResetLabel.BackgroundTransparency = 1
ResetLabel.Text = "🔄 INSTA RESET"
ResetLabel.TextColor3 = Color3.new(1, 1, 1)
ResetLabel.Font = Enum.Font.GothamBold
ResetLabel.TextSize = 12
ResetLabel.TextXAlignment = Enum.TextXAlignment.Left
ResetLabel.Parent = ResetButton

ResetButton.MouseEnter:Connect(function()
    TweenService:Create(ResetButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.new(0.18, 0.18, 0.18)}):Play()
end)
ResetButton.MouseLeave:Connect(function()
    TweenService:Create(ResetButton, TweenInfo.new(0.15), {BackgroundColor3 = Color3.new(0.117647, 0.117647, 0.117647)}):Play()
end)

ResetButton.MouseButton1Click:Connect(function()
    ExecuteReset()
end)

-- Trigger Slider
local SliderArea = Instance.new("Frame")
SliderArea.Name = "SliderArea"
SliderArea.Size = UDim2.new(1, 0, 0, 40)
SliderArea.Position = UDim2.new(0, 0, 0, 172)
SliderArea.BackgroundTransparency = 1
SliderArea.BorderSizePixel = 0
SliderArea.Parent = Content

local TriggerLabel = Instance.new("TextLabel")
TriggerLabel.Name = "TriggerLabel"
TriggerLabel.Size = UDim2.new(1, 0, 0, 16)
TriggerLabel.BackgroundTransparency = 1
TriggerLabel.Text = "TRIGGER: 100%"
TriggerLabel.TextColor3 = Color3.new(1, 1, 1)
TriggerLabel.Font = Enum.Font.GothamBold
TriggerLabel.TextSize = 10
TriggerLabel.TextXAlignment = Enum.TextXAlignment.Left
TriggerLabel.Parent = SliderArea

local SliderTrack = Instance.new("Frame")
SliderTrack.Name = "Track"
SliderTrack.Size = UDim2.new(1, 0, 0, 6)
SliderTrack.Position = UDim2.new(0, 0, 0, 20)
SliderTrack.BackgroundColor3 = Color3.new(0.156863, 0.156863, 0.156863)
SliderTrack.BorderSizePixel = 0
SliderTrack.Parent = SliderArea

local TrackCorner = Instance.new("UICorner")
TrackCorner.CornerRadius = UDim.new(1, 0)
TrackCorner.Parent = SliderTrack

local SliderFill = Instance.new("Frame")
SliderFill.Name = "Fill"
SliderFill.Size = UDim2.new(1, 0, 1, 0)
SliderFill.BackgroundColor3 = Color3.new(1, 1, 1)
SliderFill.BorderSizePixel = 0
SliderFill.Parent = SliderTrack

local FillCorner = Instance.new("UICorner")
FillCorner.CornerRadius = UDim.new(1, 0)
FillCorner.Parent = SliderFill

local SliderKnob = Instance.new("TextButton")
SliderKnob.Name = "Knob"
SliderKnob.Size = UDim2.new(0, 14, 0, 14)
SliderKnob.Position = UDim2.new(1, 0, 0.5, 0)
SliderKnob.AnchorPoint = Vector2.new(0.5, 0.5)
SliderKnob.BackgroundColor3 = Color3.new(1, 1, 1)
SliderKnob.BorderSizePixel = 0
SliderKnob.Text = ""
SliderKnob.ZIndex = 2
SliderKnob.Parent = SliderTrack

local KnobCorner = Instance.new("UICorner")
KnobCorner.CornerRadius = UDim.new(1, 0)
KnobCorner.Parent = SliderKnob

-- Slider Logic
do
    local sliderDragging = false

    local function updateSlider(inputX)
        local trackAbsPos = SliderTrack.AbsolutePosition.X
        local trackAbsSize = SliderTrack.AbsoluteSize.X
        local relativeX = math.clamp((inputX - trackAbsPos) / trackAbsSize, 0, 1)
        manualOverride = true
        sliderValue = relativeX
        SliderFill.Size = UDim2.new(relativeX, 0, 1, 0)
        SliderKnob.Position = UDim2.new(relativeX, 0, 0.5, 0)
        TriggerLabel.Text = "TRIGGER: " .. math.floor(relativeX * 100) .. "%"
    end

    SliderKnob.MouseButton1Down:Connect(function()
        sliderDragging = true
    end)

    SliderTrack.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            sliderDragging = true
            updateSlider(input.Position.X)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if sliderDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            updateSlider(input.Position.X)
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            sliderDragging = false
        end
    end)
end

-- Auto Ping Adjust
local function getPingBase(ping)
    if ping <= 30 then return 0.91
    elseif ping <= 70 then return 0.92
    elseif ping <= 120 then return 0.93
    else return 0.94 end
end

local function updateSliderVisual(v)
    sliderValue = v
    SliderFill.Size = UDim2.new(v, 0, 1, 0)
    SliderKnob.Position = UDim2.new(v, 0, 0.5, 0)
    TriggerLabel.Text = "TRIGGER: " .. math.floor(v * 100) .. "%"
end

RunService.Heartbeat:Connect(function()
    if not manualOverride then
        local ping = player:GetNetworkPing() * 1000
        local base = getPingBase(ping)
        updateSliderVisual(triggerBump and base + 0.01 or base)
    end
end)

-- =============================================
-- FLASH STEAL CORE (CatHUB Style)
-- =============================================
ProximityPromptService.PromptButtonHoldBegan:Connect(function(prompt)
    if not flashEnabled then return end
    if activeTriggers[prompt] then return end
    activeTriggers[prompt] = true
    local start = os.clock()
    local fired = false

    StealBarFill.BackgroundTransparency = 0
    StealBarBG.BackgroundTransparency = 0

    local conn
    conn = RunService.PreRender:Connect(function()
        if not prompt or not prompt.Parent then
            conn:Disconnect()
            activeTriggers[prompt] = nil
            TweenService:Create(StealBarFill, TweenInfo.new(0.3), {Size = UDim2.new(0, 0, 1, 0)}):Play()
            return
        end

        local progress = math.clamp((os.clock() - start) / prompt.HoldDuration, 0, 1)
        StealBarFill.Size = UDim2.new(progress, 0, 1, 0)

        if not fired and progress >= sliderValue then
            fired = true
            conn:Disconnect()
            activeTriggers[prompt] = nil
            triggerBump = not triggerBump
            local char = player.Character
            local tool = char and char:FindFirstChildOfClass("Tool")
            if tool then tool:Activate() end

            StealBarFill.Size = UDim2.new(1, 0, 1, 0)
            task.delay(0.2, function()
                TweenService:Create(StealBarFill, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
                    Size = UDim2.new(0, 0, 1, 0)
                }):Play()
            end)
        end
    end)

    prompt.PromptButtonHoldEnded:Connect(function()
        if not fired then
            conn:Disconnect()
            activeTriggers[prompt] = nil
            TweenService:Create(StealBarFill, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
                Size = UDim2.new(0, 0, 1, 0)
            }):Play()
        end
    end)
end)

-- =============================================
-- HOTKEYS
-- =============================================
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
    
    if input.KeyCode == Enum.KeyCode.F then
        ExecuteSequentialTeleport()
    elseif input.KeyCode == Enum.KeyCode.R then
        ExecuteReset()
    end
end)

print("========================================")
print("✅ LAW HUB - FLASH STEAL + TELEPORT LOADED!")
print("📍 Teleports: 1 → 10 sequential")
print("🎮 Controls:")
print("   - F Key: Execute sequential teleport")
print("   - R Key: Insta reset")
print("   - FLASH TP: Auto steal with trigger slider")
print("========================================")
