-- =============================================
-- LAW HUB - TELEPORT SYSTEM (TAPETE + FLASH)
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- =============================================
-- PONTO DE PARTIDA (SÓ PARA TELEPORT 2)
-- =============================================
local PontoPartida = Vector3.new(-357.6, -7.1, 37.4)

-- =============================================
-- LISTA DE TELEPORTS
-- =============================================
local Teleports = {
    ["📌 TELEPORT 1"] = {
        UsarPartida = false,
        Pontos = {
            Vector3.new(-324.3, -7.1, 37.5),
            Vector3.new(-316.1, -7.1, 36.7),
            Vector3.new(-309.8, -7.1, 38.3),
            Vector3.new(-302.3, -7.1, 41.5),
        }
    },
    
    ["📌 TELEPORT 2"] = {
        UsarPartida = true,
        Pontos = {
            Vector3.new(-331.6, -7.1, -23.3),
            Vector3.new(-324.1, -7.1, -23.2),
            Vector3.new(-317.8, -7.1, -23.6),
            Vector3.new(-311.1, -7.1, -23.5),
            Vector3.new(-302.6, -7.1, -23.9),
        }
    },
}

-- =============================================
-- VARIÁVEIS
-- =============================================
local RootPart = nil
local Humanoid = nil
local teleportando = false
local teleportSelecionado = nil

-- =============================================
-- FUNÇÕES DO PERSONAGEM
-- =============================================
local function AtualizarPersonagem()
    local char = LocalPlayer.Character
    if char then
        RootPart = char:FindFirstChild("HumanoidRootPart")
        Humanoid = char:FindFirstChildOfClass("Humanoid")
    end
end

LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    AtualizarPersonagem()
end)
AtualizarPersonagem()

-- =============================================
-- EQUIPAR TAPETE VOADOR
-- =============================================
local function EquiparTapete()
    local char = LocalPlayer.Character
    local bp = LocalPlayer:FindFirstChild("Backpack")
    if not char or not bp then return end
    
    -- Guarda todas as tools na mochila
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") then
            tool.Parent = bp
        end
    end
    
    -- Equipa o tapete
    local tapete = bp:FindFirstChild("Flying Carpet")
    if tapete and Humanoid then
        Humanoid:EquipTool(tapete)
        task.wait(0.2)
    end
end

-- =============================================
-- USAR FLASH
-- =============================================
local function UsarFlash()
    local char = LocalPlayer.Character
    local bp = LocalPlayer:FindFirstChild("Backpack")
    
    if not char then return end
    
    -- Procura o Flash
    local flash = nil
    for _, tool in pairs(char:GetChildren()) do
        if tool:IsA("Tool") and (tool.Name:lower():find("flash") or tool.Name:lower():find("flashlight")) then
            flash = tool
            break
        end
    end
    
    if not flash and bp then
        for _, tool in pairs(bp:GetChildren()) do
            if tool:IsA("Tool") and (tool.Name:lower():find("flash") or tool.Name:lower():find("flashlight")) then
                flash = tool
                break
            end
        end
    end
    
    -- Usa o Flash
    if flash then
        if flash.Parent == bp then
            flash.Parent = char
            task.wait(0.1)
        end
        pcall(function() 
            flash:Activate() 
        end)
    end
end

-- =============================================
-- TELEPORTAR PARA POSIÇÃO
-- =============================================
local function TeleportarPosicao(posicao)
    if not RootPart then return false end
    RootPart.CFrame = CFrame.new(posicao)
    RootPart.AssemblyLinearVelocity = Vector3.zero
    return true
end

-- =============================================
-- EXECUTAR TELEPORT COMPLETO
-- =============================================
local function ExecutarTeleport(nomeTeleport)
    if teleportando then
        print("⚠️ Já está teleportando...")
        return
    end
    
    local tp = Teleports[nomeTeleport]
    if not tp then
        print("❌ Teleport não encontrado: " .. nomeTeleport)
        return
    end
    
    teleportando = true
    
    -- PASSO 1: Equipa o Tapete Voador
    print("🔧 Equipando Tapete Voador...")
    EquiparTapete()
    task.wait(0.2)
    
    -- PASSO 2: Usa o Flash
    print("⚡ Usando Flash...")
    UsarFlash()
    task.wait(0.1)
    
    -- PASSO 3: Se precisar, vai pro ponto de partida
    if tp.UsarPartida then
        print("📍 Teleportando para ponto de partida...")
        TeleportarPosicao(PontoPartida)
        task.wait(0.15)
    end
    
    -- PASSO 4: Teleporta por todos os pontos da rota
    for i, ponto in ipairs(tp.Pontos) do
        TeleportarPosicao(ponto)
        print("📍 Teleport " .. i .. "/" .. #tp.Pontos .. ": " .. tostring(ponto))
        task.wait(0.15)
    end
    
    print("✅ " .. nomeTeleport .. " concluído!")
    
    -- Notificação
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "✅ Teleport Concluído!";
        Text = nomeTeleport;
        Duration = 2;
    })
    
    teleportando = false
end

-- =============================================
-- GUI PRINCIPAL (COM ABAS TÓPICOS)
-- =============================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LawHub"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- Frame Principal
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 350, 0, 500)
MainFrame.Position = UDim2.new(0.5, -175, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 30, 40)
MainFrame.BackgroundTransparency = 0.05
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner", MainFrame)
MainCorner.CornerRadius = UDim.new(0, 15)

-- Título
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "🔥 LAW HUB - TELEPORT"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 20
Title.TextColor3 = Color3.fromRGB(255, 255, 255)

-- Subtítulo
local SubTitle = Instance.new("TextLabel", MainFrame)
SubTitle.Size = UDim2.new(1, 0, 0, 20)
SubTitle.Position = UDim2.new(0, 0, 0, 45)
SubTitle.BackgroundTransparency = 1
SubTitle.Text = "Tapete Voador + Flash"
SubTitle.Font = Enum.Font.Gotham
SubTitle.TextSize = 11
SubTitle.TextColor3 = Color3.fromRGB(180, 180, 180)

-- =============================================
-- SISTEMA DE ABAS (TÓPICOS)
-- =============================================
local AbasFrame = Instance.new("Frame", MainFrame)
AbasFrame.Position = UDim2.new(0, 10, 0, 70)
AbasFrame.Size = UDim2.new(1, -20, 0, 40)
AbasFrame.BackgroundTransparency = 1

local AbasLayout = Instance.new("UIListLayout", AbasFrame)
AbasLayout.FillDirection = Enum.FillDirection.Horizontal
AbasLayout.Padding = UDim.new(0, 10)
AbasLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

-- Painel de Conteúdo (muda conforme aba)
local ContentFrame = Instance.new("Frame", MainFrame)
ContentFrame.Position = UDim2.new(0, 10, 0, 120)
ContentFrame.Size = UDim2.new(1, -20, 1, -175)
ContentFrame.BackgroundTransparency = 1

-- Botão Executar (fora das abas)
local ExecutarBtn = Instance.new("TextButton", MainFrame)
ExecutarBtn.Position = UDim2.new(0, 10, 1, -55)
ExecutarBtn.Size = UDim2.new(1, -20, 0, 45)
ExecutarBtn.BackgroundColor3 = Color3.fromRGB(80, 120, 200)
ExecutarBtn.Text = "🚀 EXECUTAR TELEPORT SELECIONADO"
ExecutarBtn.Font = Enum.Font.GothamBold
ExecutarBtn.TextSize = 14
ExecutarBtn.TextColor3 = Color3.fromRGB(255, 255, 255)

local ExecutarCorner = Instance.new("UICorner", ExecutarBtn)
ExecutarCorner.CornerRadius = UDim.new(0, 8)

-- Limpar conteúdo
local function LimparConteudo()
    for _, child in pairs(ContentFrame:GetChildren()) do
        if child:IsA("ScrollingFrame") then
            child:Destroy()
        end
    end
end

-- =============================================
-- ABA: TELEPORTS
-- =============================================
local function CriarAbaTeleports()
    LimparConteudo()
    
    local Scroll = Instance.new("ScrollingFrame", ContentFrame)
    Scroll.Size = UDim2.new(1, 0, 1, 0)
    Scroll.BackgroundTransparency = 1
    Scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    Scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    Scroll.ScrollBarThickness = 4
    
    local Layout = Instance.new("UIListLayout", Scroll)
    Layout.Padding = UDim.new(0, 12)
    Layout.SortOrder = Enum.SortOrder.LayoutOrder
    
    -- Mostrar Ponto de Partida
    local PartidaFrame = Instance.new("Frame", Scroll)
    PartidaFrame.Size = UDim2.new(1, 0, 0, 50)
    PartidaFrame.BackgroundColor3 = Color3.fromRGB(60, 70, 90)
    
    local PartidaCorner = Instance.new("UICorner", PartidaFrame)
    PartidaCorner.CornerRadius = UDim.new(0, 8)
    
    local PartidaLabel = Instance.new("TextLabel", PartidaFrame)
    PartidaLabel.Size = UDim2.new(1, -10, 0, 20)
    PartidaLabel.Position = UDim2.new(0, 5, 0, 5)
    PartidaLabel.BackgroundTransparency = 1
    PartidaLabel.Text = "🚩 PONTO DE PARTIDA (TELEPORT 2)"
    PartidaLabel.Font = Enum.Font.GothamBold
    PartidaLabel.TextSize = 11
    PartidaLabel.TextColor3 = Color3.fromRGB(255, 200, 100)
    PartidaLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local PartidaPos = Instance.new("TextLabel", PartidaFrame)
    PartidaPos.Size = UDim2.new(1, -10, 0, 20)
    PartidaPos.Position = UDim2.new(0, 5, 0, 25)
    PartidaPos.BackgroundTransparency = 1
    PartidaPos.Text = tostring(PontoPartida)
    PartidaPos.Font = Enum.Font.Gotham
    PartidaPos.TextSize = 10
    PartidaPos.TextColor3 = Color3.fromRGB(180, 180, 180)
    PartidaPos.TextXAlignment = Enum.TextXAlignment.Left
    
    -- Mostrar todos os Teleports
    for nomeTeleport, tp in pairs(Teleports) do
        local TeleportFrame = Instance.new("Frame", Scroll)
        TeleportFrame.Size = UDim2.new(1, 0, 0, 90)
        TeleportFrame.BackgroundColor3 = Color3.fromRGB(45, 50, 65)
        
        local TeleportCorner = Instance.new("UICorner", TeleportFrame)
        TeleportCorner.CornerRadius = UDim.new(0, 10)
        
        -- Nome do Teleport
        local NomeLabel = Instance.new("TextLabel", TeleportFrame)
        NomeLabel.Size = UDim2.new(1, -10, 0, 30)
        NomeLabel.Position = UDim2.new(0, 5, 0, 5)
        NomeLabel.BackgroundTransparency = 1
        NomeLabel.Text = nomeTeleport
        NomeLabel.Font = Enum.Font.GothamBold
        NomeLabel.TextSize = 14
        NomeLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        NomeLabel.TextXAlignment = Enum.TextXAlignment.Left
        
        -- Info dos pontos
        local InfoLabel = Instance.new("TextLabel", TeleportFrame)
        InfoLabel.Size = UDim2.new(1, -10, 0, 20)
        InfoLabel.Position = UDim2.new(0, 5, 0, 35)
        InfoLabel.BackgroundTransparency = 1
        InfoLabel.Text = #tp.Pontos .. " pontos na rota" .. (tp.UsarPartida and " | Começa pelo ponto de partida" or "")
        InfoLabel.Font = Enum.Font.Gotham
        InfoLabel.TextSize = 10
        InfoLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
        InfoLabel.TextXAlignment = Enum.TextXAlignment.Left
        
        -- Botão de Selecionar
        local SelectBtn = Instance.new("TextButton", TeleportFrame)
        SelectBtn.Size = UDim2.new(1, -10, 0, 30)
        SelectBtn.Position = UDim2.new(0, 5, 0, 55)
        SelectBtn.BackgroundColor3 = Color3.fromRGB(60, 70, 90)
        SelectBtn.Text = "📌 SELECIONAR"
        SelectBtn.Font = Enum.Font.GothamBold
        SelectBtn.TextSize = 12
        SelectBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        
        local SelectCorner = Instance.new("UICorner", SelectBtn)
        SelectCorner.CornerRadius = UDim.new(0, 6)
        
        -- Marca de seleção (check)
        local CheckMark = Instance.new("TextLabel", TeleportFrame)
        CheckMark.Size = UDim2.new(0, 30, 0, 30)
        CheckMark.Position = UDim2.new(1, -35, 0, 5)
        CheckMark.BackgroundTransparency = 1
        CheckMark.Text = "⬜"
        CheckMark.Font = Enum.Font.GothamBold
        CheckMark.TextSize = 20
        CheckMark.TextColor3 = Color3.fromRGB(150, 150, 150)
        
        SelectBtn.MouseButton1Click:Connect(function()
            teleportSelecionado = nomeTeleport
            -- Resetar todos os checks
            for _, frame in pairs(Scroll:GetChildren()) do
                if frame:IsA("Frame") and frame:FindFirstChild("CheckMark") then
                    frame.CheckMark.Text = "⬜"
                    frame.CheckMark.TextColor3 = Color3.fromRGB(150, 150, 150)
                end
            end
            CheckMark.Text = "✅"
            CheckMark.TextColor3 = Color3.fromRGB(100, 255, 100)
            
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "📌 Teleport Selecionado";
                Text = nomeTeleport;
                Duration = 1;
            })
        end)
    end
end

-- =============================================
-- ABA: INFO/CONFIG
-- =============================================
local function CriarAbaInfo()
    LimparConteudo()
    
    local Scroll = Instance.new("ScrollingFrame", ContentFrame)
    Scroll.Size = UDim2.new(1, 0, 1, 0)
    Scroll.BackgroundTransparency = 1
    Scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    Scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    Scroll.ScrollBarThickness = 4
    
    local Layout = Instance.new("UIListLayout", Scroll)
    Layout.Padding = UDim.new(0, 10)
    
    -- Informações
    local InfoFrame = Instance.new("Frame", Scroll)
    InfoFrame.Size = UDim2.new(1, 0, 0, 150)
    InfoFrame.BackgroundColor3 = Color3.fromRGB(45, 50, 65)
    
    local InfoCorner = Instance.new("UICorner", InfoFrame)
    InfoCorner.CornerRadius = UDim.new(0, 8)
    
    local InfoTitle = Instance.new("TextLabel", InfoFrame)
    InfoTitle.Size = UDim2.new(1, -10, 0, 30)
    InfoTitle.Position = UDim2.new(0, 5, 0, 5)
    InfoTitle.BackgroundTransparency = 1
    InfoTitle.Text = "⚙️ INFORMAÇÕES"
    InfoTitle.Font = Enum.Font.GothamBold
    InfoTitle.TextSize = 13
    InfoTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
    InfoTitle.TextXAlignment = Enum.TextXAlignment.Left
    
    local InfoText = Instance.new("TextLabel", InfoFrame)
    InfoText.Size = UDim2.new(1, -10, 0, 100)
    InfoText.Position = UDim2.new(0, 5, 0, 40)
    InfoText.BackgroundTransparency = 1
    InfoText.Text = "🔧 COMO FUNCIONA:\n\n• Tapete Voador equipado automaticamente\n• Flash usado antes do teleporte\n• Ponto de partida: -357.6, -7.1, 37.4\n• Delay entre pontos: 0.15s\n\n🎮 CONTROLES:\n• Clique em SELECIONAR no teleport desejado\n• Clique em EXECUTAR ou aperte F"
    InfoText.Font = Enum.Font.Gotham
    InfoText.TextSize = 11
    InfoText.TextColor3 = Color3.fromRGB(180, 180, 180)
    InfoText.TextXAlignment = Enum.TextXAlignment.Left
    InfoText.TextYAlignment = Enum.TextYAlignment.Top
    InfoText.LineHeight = 1.2
    
    -- Tecla de atalho
    local HotkeyFrame = Instance.new("Frame", Scroll)
    HotkeyFrame.Size = UDim2.new(1, 0, 0, 50)
    HotkeyFrame.BackgroundColor3 = Color3.fromRGB(45, 50, 65)
    
    local HotkeyCorner = Instance.new("UICorner", HotkeyFrame)
    HotkeyCorner.CornerRadius = UDim.new(0, 8)
    
    local HotkeyLabel = Instance.new("TextLabel", HotkeyFrame)
    HotkeyLabel.Size = UDim2.new(0.5, 0, 1, 0)
    HotkeyLabel.Position = UDim2.new(0, 10, 0, 0)
    HotkeyLabel.BackgroundTransparency = 1
    HotkeyLabel.Text = "⌨️ TECLA DE ATALHO"
    HotkeyLabel.Font = Enum.Font.GothamBold
    HotkeyLabel.TextSize = 12
    HotkeyLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    HotkeyLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local HotkeyDisplay = Instance.new("TextLabel", HotkeyFrame)
    HotkeyDisplay.Size = UDim2.new(0, 50, 0, 30)
    HotkeyDisplay.Position = UDim2.new(1, -60, 0.5, -15)
    HotkeyDisplay.BackgroundColor3 = Color3.fromRGB(60, 70, 90)
    HotkeyDisplay.Text = "F"
    HotkeyDisplay.Font = Enum.Font.GothamBold
    HotkeyDisplay.TextSize = 16
    HotkeyDisplay.TextColor3 = Color3.fromRGB(255, 200, 100)
    
    local HotkeyCorner2 = Instance.new("UICorner", HotkeyDisplay)
    HotkeyCorner2.CornerRadius = UDim.new(0, 8)
end

-- =============================================
-- CRIAR ABAS
-- =============================================
local function CriarAbas()
    -- Aba Teleports
    local AbaTeleports = Instance.new("TextButton", AbasFrame)
    AbaTeleports.Size = UDim2.new(0, 130, 1, 0)
    AbaTeleports.BackgroundColor3 = Color3.fromRGB(80, 100, 150)
    AbaTeleports.Text = "📍 TELEPORTS"
    AbaTeleports.Font = Enum.Font.GothamBold
    AbaTeleports.TextSize = 13
    
    local AbaTeleportsCorner = Instance.new("UICorner", AbaTeleports)
    AbaTeleportsCorner.CornerRadius = UDim.new(0, 8)
    
    -- Aba Info
    local AbaInfo = Instance.new("TextButton", AbasFrame)
    AbaInfo.Size = UDim2.new(0, 130, 1, 0)
    AbaInfo.BackgroundColor3 = Color3.fromRGB(45, 50, 65)
    AbaInfo.Text = "ℹ️ INFO"
    AbaInfo.Font = Enum.Font.GothamBold
    AbaInfo.TextSize = 13
    
    local AbaInfoCorner = Instance.new("UICorner", AbaInfo)
    AbaInfoCorner.CornerRadius = UDim.new(0, 8)
    
    -- Funções das abas
    AbaTeleports.MouseButton1Click:Connect(function()
        AbaTeleports.BackgroundColor3 = Color3.fromRGB(80, 100, 150)
        AbaInfo.BackgroundColor3 = Color3.fromRGB(45, 50, 65)
        CriarAbaTeleports()
    end)
    
    AbaInfo.MouseButton1Click:Connect(function()
        AbaInfo.BackgroundColor3 = Color3.fromRGB(80, 100, 150)
        AbaTeleports.BackgroundColor3 = Color3.fromRGB(45, 50, 65)
        CriarAbaInfo()
    end)
    
    -- Iniciar com aba de teleports
    CriarAbaTeleports()
end

CriarAbas()

-- Botão Executar
ExecutarBtn.MouseButton1Click:Connect(function()
    if teleportSelecionado then
        ExecutarTeleport(teleportSelecionado)
    else
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "⚠️ Atenção!";
            Text = "Selecione um teleport primeiro!";
            Duration = 2;
        })
    end
end)

-- Hotkey F para executar
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.F and teleportSelecionado then
        ExecutarTeleport(teleportSelecionado)
    end
end)

-- =============================================
-- DRAG SYSTEM (MOVER A GUI)
-- =============================================
local dragToggle = false
local dragStart = nil
local startPos = nil

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragToggle = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragToggle and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragToggle = false
    end
end)

print("========================================")
print("✅ LAW HUB CARREGADO!")
print("📍 TELEPORT 1: 4 pontos (sem partida)")
print("📍 TELEPORT 2: 5 pontos (com partida)")
print("🎮 Selecione o teleport e aperte F ou clique em EXECUTAR")
print("========================================")
