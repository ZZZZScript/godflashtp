-- =============================================
-- LAW HUB - VERSÃO FINAL (GARANTIDO)
-- =============================================

-- ESPERA TUDO CARREGAR
wait(3)

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

-- ESPERA O PLAYERGUI EXISTIR
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- CONFIGURAÇÕES
local Toggles = {
    InstantSteal = false,
    AutoGrab = false,
    ActivePotion = false,
    UnlockBase = false,
    StealSpeed = false,
}

local Values = {
    SpeedAmount = 28,
}

-- VARIAVEIS
local RootPart = nil
local Humanoid = nil
local teleporting = false
local PotionOnTeleport = false
local listening = false
local currentKeybind = Enum.KeyCode.F
local speedConn = nil
local StealPanel = nil
local UnlockPanel = nil
local ScreenGui = nil
local MainFrame = nil

-- =============================================
-- CORNER
-- =============================================
local function corner(obj, r)
    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, r or 8)
    c.Parent = obj
end

-- =============================================
-- PERSONAGEM
-- =============================================
local function updateChar(char)
    task.wait(0.5)
    RootPart = char:FindFirstChild("HumanoidRootPart")
    Humanoid = char:FindFirstChildOfClass("Humanoid")
end

LocalPlayer.CharacterAdded:Connect(updateChar)

local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
RootPart = char:FindFirstChild("HumanoidRootPart")
Humanoid = char:FindFirstChildOfClass("Humanoid")

-- =============================================
-- ADMIN PANEL
-- =============================================
local function GetAdminFrames()
    local ap = LocalPlayer.PlayerGui:FindFirstChild("AdminPanel")
    if not ap then return nil, nil end
    local pnl = ap:FindFirstChild("AdminPanel")
    if not pnl then return nil, nil end
    local cont = pnl:FindFirstChild("Content")
    local prof = pnl:FindFirstChild("Profiles")
    if not cont or not prof then return nil, nil end
    return cont:FindFirstChild("ScrollingFrame"), prof:FindFirstChild("ScrollingFrame")
end

local function FireButton(btn)
    local ok, conns = pcall(getconnections, btn.Activated)
    if ok and conns then
        for _, c in ipairs(conns) do
            pcall(task.spawn, c.Function)
        end
    end
end

local function RunCommand(cmd, target)
    local cmdFrame, profFrame = GetAdminFrames()
    if not cmdFrame or not profFrame then return end
    local profBtn = profFrame:FindFirstChild(target.Name)
    local cmdBtn = cmdFrame:FindFirstChild(cmd)
    if profBtn and cmdBtn then
        FireButton(profBtn)
        task.wait(0.05)
        FireButton(cmdBtn)
    end
end

-- =============================================
-- POÇÃO
-- =============================================
local function UsePotion()
    local bp = LocalPlayer:FindFirstChild("Backpack")
    local char = LocalPlayer.Character
    if bp and char then
        local pot = bp:FindFirstChild("Giant Potion")
        if pot then
            pot.Parent = char
            pcall(function() pot:Activate() end)
        end
    end
end

-- =============================================
-- TELEPORT
-- =============================================
local function GetMyPlot()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    for _, p in pairs(plots:GetChildren()) do
        local sign = p:FindFirstChild("PlotSign")
        if sign and sign:FindFirstChild("YourBase") then
            return p
        end
    end
    return nil
end

local function TeleportTo(pos)
    if RootPart then
        RootPart.CFrame = CFrame.new(pos)
        RootPart.AssemblyLinearVelocity = Vector3.zero
    end
end

local function EquipCarpet()
    local char = LocalPlayer.Character
    local bp = LocalPlayer:FindFirstChild("Backpack")
    if not char or not bp then return
    for _, t in pairs(char:GetChildren()) do
        if t:IsA("Tool") then
            t.Parent = bp
        end
    end
    local carpet = bp:FindFirstChild("Flying Carpet")
    if carpet and Humanoid then
        Humanoid:EquipTool(carpet)
    end
end

local function DoTeleport()
    if teleporting then return end
    teleporting = true
    
    if PotionOnTeleport then
        UsePotion()
    end
    
    local plot = GetMyPlot()
    if plot then
        EquipCarpet()
        task.wait(0.1)
        local order = plot:GetAttribute("Order")
        if order == 2 then
            TeleportTo(Vector3.new(-368, -7, 69))
            task.wait(0.1)
            TeleportTo(Vector3.new(-336, -5, 100))
            task.wait(0.1)
            TeleportTo(Vector3.new(-352, -7, 76))
        else
            TeleportTo(Vector3.new(-368, -7, 42))
            task.wait(0.1)
            TeleportTo(Vector3.new(-336, -5, 20))
            task.wait(0.1)
            TeleportTo(Vector3.new(-353, -7, 44))
        end
        
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= LocalPlayer then
                RunCommand("steal", plr)
                break
            end
        end
    end
    
    task.wait(0.5)
    teleporting = false
end

-- =============================================
-- AUTO GRAB
-- =============================================
local function FindPrompt()
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    
    local nearest, minD = nil, math.huge
    local plots = workspace:FindFirstChild("Plots")
    if plots then
        for _, desc in pairs(plots:GetDescendants()) do
            if desc:IsA("ProximityPrompt") and desc.Enabled and desc.ActionText == "Steal" then
                local part = desc.Parent
                local pos = nil
                if part:IsA("BasePart") then
                    pos = part.Position
                elseif part:IsA("Model") and part.PrimaryPart then
                    pos = part.PrimaryPart.Position
                end
                if pos then
                    local d = (hrp.Position - pos).Magnitude
                    if d < minD then
                        minD = d
                        nearest = desc
                    end
                end
            end
        end
    end
    return nearest
end

local function StealPrompt(p)
    if not p then return
    p.MaxActivationDistance = 9999
    p.RequiresLineOfSight = false
    pcall(function() fireproximityprompt(p, 9999, 0.3) end)
end

task.spawn(function()
    while true do
        if Toggles.AutoGrab then
            local p = FindPrompt()
            if p then StealPrompt(p) end
        end
        task.wait(0.15)
    end
end)

-- =============================================
-- ACTIVE POTION
-- =============================================
local palavras = {"steal", "stole", "roubou", "pegou", "robou"}

task.spawn(function()
    while true do
        if Toggles.ActivePotion then
            local gui = LocalPlayer:FindFirstChild("PlayerGui")
            if gui then
                for _, obj in pairs(gui:GetDescendants()) do
                    if obj:IsA("TextLabel") and obj.Text then
                        local txt = string.lower(obj.Text)
                        for _, p in ipairs(palavras) do
                            if string.find(txt, p) then
                                UsePotion()
                                break
                            end
                        end
                    end
                end
            end
        end
        task.wait(0.3)
    end
end)

-- =============================================
-- STEAL SPEED
-- =============================================
local function StartSpeed()
    if speedConn then speedConn:Disconnect() end
    speedConn = RunService.Heartbeat:Connect(function()
        if Toggles.StealSpeed and Toggles.InstantSteal then
            local char = LocalPlayer.Character
            if char then
                local hrp = char:FindFirstChild("HumanoidRootPart")
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hrp and hum then
                    hum.WalkSpeed = Values.SpeedAmount
                    local dir = hum.MoveDirection
                    if dir.Magnitude > 0 then
                        hrp.AssemblyLinearVelocity = Vector3.new(dir.X * Values.SpeedAmount, hrp.AssemblyLinearVelocity.Y, dir.Z * Values.SpeedAmount)
                    end
                end
            end
        end
    end)
end

local function StopSpeed()
    if speedConn then
        speedConn:Disconnect()
        speedConn = nil
    end
    local char = LocalPlayer.Character
    if char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = 16 end
    end
end

-- =============================================
-- UNLOCK BASE
-- =============================================
local function FindOtherPlot()
    local plots = workspace:FindFirstChild("Plots")
    if not plots then return nil end
    for _, p in pairs(plots:GetChildren()) do
        local sign = p:FindFirstChild("PlotSign")
        if sign then
            local yb = sign:FindFirstChild("YourBase")
            if not (yb and yb.Enabled) then
                return p
            end
        end
    end
    return nil
end

local function DoUnlock()
    local plot = FindOtherPlot()
    if plot then
        for _, obj in pairs(plot:GetDescendants()) do
            if obj:IsA("ProximityPrompt") and obj.Enabled then
                local txt = (obj.ActionText or ""):lower()
                if string.find(txt, "unlock") then
                    local old = obj.MaxActivationDistance
                    obj.MaxActivationDistance = 9999
                    pcall(function() fireproximityprompt(obj, 9999) end)
                    task.delay(0.3, function() obj.MaxActivationDistance = old end)
                    break
                end
            end
        end
    end
end

-- =============================================
-- PAINEL UNLOCK
-- =============================================
local function ShowUnlock(show)
    if show then
        if UnlockPanel then UnlockPanel:Destroy() end
        local sg = Instance.new("ScreenGui")
        sg.Name = "LawUnlock"
        sg.ResetOnSpawn = false
        sg.Parent = PlayerGui
        
        local f = Instance.new("Frame")
        f.Size = UDim2.new(0, 220, 0, 100)
        f.Position = UDim2.new(0.5, -110, 0.65, 0)
        f.BackgroundColor3 = Color3.fromRGB(45, 65, 80)
        f.BackgroundTransparency = 0.1
        f.BorderSizePixel = 0
        f.Active = true
        f.Draggable = true
        f.Parent = sg
        corner(f, 12)
        
        local t = Instance.new("TextLabel", f)
        t.Size = UDim2.new(1, 0, 0, 35)
        t.BackgroundTransparency = 1
        t.Text = "UNLOCK BASE"
        t.Font = Enum.Font.GothamBold
        t.TextSize = 14
        t.TextColor3 = Color3.fromRGB(255, 255, 255)
        
        local cont = Instance.new("Frame", f)
        cont.Size = UDim2.new(1, -16, 1, -45)
        cont.Position = UDim2.new(0, 8, 0, 40)
        cont.BackgroundTransparency = 1
        
        local layout = Instance.new("UIListLayout", cont)
        layout.Padding = UDim.new(0, 10)
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        
        for i = 1, 3 do
            local btn = Instance.new("TextButton", cont)
            btn.Size = UDim2.new(0, 60, 0, 45)
            btn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
            btn.Text = tostring(i)
            btn.Font = Enum.Font.GothamBold
            btn.TextSize = 20
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            corner(btn, 8)
            btn.MouseButton1Click:Connect(DoUnlock)
        end
        UnlockPanel = sg
    else
        if UnlockPanel then UnlockPanel:Destroy(); UnlockPanel = nil end
    end
end

-- =============================================
-- PAINEL STEAL
-- =============================================
local function ShowSteal(show)
    if show then
        if StealPanel then StealPanel:Destroy() end
        local sg = Instance.new("ScreenGui")
        sg.Name = "LawSteal"
        sg.ResetOnSpawn = false
        sg.Parent = PlayerGui
        
        local f = Instance.new("Frame")
        f.Size = UDim2.new(0, 260, 0, 300)
        f.Position = UDim2.new(0.5, -130, 0.3, 0)
        f.BackgroundColor3 = Color3.fromRGB(45, 65, 80)
        f.BackgroundTransparency = 0.1
        f.BorderSizePixel = 0
        f.Active = true
        f.Draggable = true
        f.Parent = sg
        corner(f, 12)
        
        local t = Instance.new("TextLabel", f)
        t.Size = UDim2.new(1, 0, 0, 35)
        t.BackgroundTransparency = 1
        t.Text = "Instant Steal TP"
        t.Font = Enum.Font.GothamBold
        t.TextSize = 14
        t.TextColor3 = Color3.fromRGB(255, 255, 255)
        
        -- Keybind
        local kbBtn = Instance.new("TextButton", f)
        kbBtn.Size = UDim2.new(1, -16, 0, 30)
        kbBtn.Position = UDim2.new(0, 8, 0, 55)
        kbBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        kbBtn.Text = "[F]"
        kbBtn.Font = Enum.Font.GothamBold
        kbBtn.TextSize = 13
        kbBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        corner(kbBtn, 6)
        
        kbBtn.MouseButton1Click:Connect(function()
            if listening then return
            listening = true
            kbBtn.Text = "..."
            local conn
            conn = UserInputService.InputBegan:Connect(function(input)
                if input.UserInputType ~= Enum.UserInputType.Keyboard then return
                conn:Disconnect()
                currentKeybind = input.KeyCode
                kbBtn.Text = "[" .. currentKeybind.Name .. "]"
                listening = false
            end)
        end)
        
        -- Teleport
        local teleBtn = Instance.new("TextButton", f)
        teleBtn.Size = UDim2.new(1, -16, 0, 35)
        teleBtn.Position = UDim2.new(0, 8, 0, 95)
        teleBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        teleBtn.Text = "Teleport"
        teleBtn.Font = Enum.Font.GothamBold
        teleBtn.TextSize = 13
        teleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        corner(teleBtn, 6)
        teleBtn.MouseButton1Click:Connect(DoTeleport)
        
        -- Reset
        local resetBtn = Instance.new("TextButton", f)
        resetBtn.Size = UDim2.new(1, -16, 0, 35)
        resetBtn.Position = UDim2.new(0, 8, 0, 135)
        resetBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        resetBtn.Text = "Reset"
        resetBtn.Font = Enum.Font.GothamBold
        resetBtn.TextSize = 13
        resetBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        corner(resetBtn, 6)
        resetBtn.MouseButton1Click:Connect(function()
            if LocalPlayer.Character then LocalPlayer.Character:Destroy() end
        end)
        
        -- Separador
        local sep = Instance.new("Frame", f)
        sep.Size = UDim2.new(1, -16, 0, 1)
        sep.Position = UDim2.new(0, 8, 0, 180)
        sep.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        sep.BackgroundTransparency = 0.5
        
        -- Potion Steal
        local potSw = Instance.new("Frame", f)
        potSw.Size = UDim2.new(0, 40, 0, 20)
        potSw.Position = UDim2.new(0.85, 0, 0.68, 0)
        potSw.BackgroundColor3 = PotionOnTeleport and Color3.fromRGB(100, 150, 200) or Color3.fromRGB(80, 80, 90)
        corner(potSw, 10)
        
        local potCircle = Instance.new("Frame", potSw)
        potCircle.Size = UDim2.new(0, 16, 0, 16)
        potCircle.Position = PotionOnTeleport and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)
        potCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        corner(potCircle, 8)
        
        potSw.MouseButton1Click:Connect(function()
            PotionOnTeleport = not PotionOnTeleport
            local targetPos = PotionOnTeleport and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)
            TweenService:Create(potCircle, TweenInfo.new(0.2), {Position = targetPos}):Play()
            potSw.BackgroundColor3 = PotionOnTeleport and Color3.fromRGB(100, 150, 200) or Color3.fromRGB(80, 80, 90)
        end)
        
        local potLabel = Instance.new("TextLabel", f)
        potLabel.Size = UDim2.new(0, 100, 0, 20)
        potLabel.Position = UDim2.new(0, 8, 0, 195)
        potLabel.BackgroundTransparency = 1
        potLabel.Text = "Potion Steal"
        potLabel.Font = Enum.Font.Gotham
        potLabel.TextSize = 12
        potLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
        
        -- Speed Slider
        local sldLabel = Instance.new("TextLabel", f)
        sldLabel.Size = UDim2.new(1, -16, 0, 15)
        sldLabel.Position = UDim2.new(0, 8, 0, 230)
        sldLabel.BackgroundTransparency = 1
        sldLabel.Text = "Steal Speed: " .. Values.SpeedAmount
        sldLabel.Font = Enum.Font.Gotham
        sldLabel.TextSize = 11
        sldLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        
        local sldBar = Instance.new("Frame", f)
        sldBar.Size = UDim2.new(1, -16, 0, 4)
        sldBar.Position = UDim2.new(0, 8, 0, 252)
        sldBar.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        corner(sldBar, 2)
        
        local percent = (Values.SpeedAmount - 26) / 10
        local sldFill = Instance.new("Frame", sldBar)
        sldFill.Size = UDim2.new(percent, 0, 1, 0)
        sldFill.BackgroundColor3 = Color3.fromRGB(100, 150, 200)
        corner(sldFill, 2)
        
        local sldKnob = Instance.new("Frame", sldBar)
        sldKnob.Size = UDim2.new(0, 12, 0, 12)
        sldKnob.Position = UDim2.new(percent, -6, 0.5, -6)
        sldKnob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        corner(sldKnob, 6)
        
        local dragging = false
        sldKnob.InputBegan:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true end
        end)
        UserInputService.InputEnded:Connect(function() dragging = false end)
        UserInputService.InputChanged:Connect(function(i)
            if dragging and i.UserInputType == Enum.UserInputType.MouseMovement then
                local p = math.clamp((i.Position.X - sldBar.AbsolutePosition.X) / sldBar.AbsoluteSize.X, 0, 1)
                sldFill.Size = UDim2.new(p, 0, 1, 0)
                sldKnob.Position = UDim2.new(p, -6, 0.5, -6)
                Values.SpeedAmount = math.floor(26 + 10 * p)
                sldLabel.Text = "Steal Speed: " .. Values.SpeedAmount
            end
        end)
        
        StealPanel = sg
    else
        if StealPanel then StealPanel:Destroy(); StealPanel = nil end
        StopSpeed()
    end
end

-- =============================================
-- TOGGLE
-- =============================================
local function CreateToggle(parent, name, configKey, hasInput)
    local Row = Instance.new("Frame")
    local Label = Instance.new("TextLabel")
    local ToggleBtn = Instance.new("TextButton")
    local Circle = Instance.new("Frame")
    
    Row.Name = name
    Row.Parent = parent
    Row.BackgroundTransparency = 1
    Row.Size = UDim2.new(1, 0, 0, 50)

    Label.Parent = Row
    Label.BackgroundTransparency = 1
    Label.Size = UDim2.new(0.5, 0, 1, 0)
    Label.Font = Enum.Font.GothamBold
    Label.Text = name
    Label.TextColor3 = Color3.fromRGB(255, 255, 255)
    Label.TextSize = 15
    Label.TextXAlignment = Enum.TextXAlignment.Left

    ToggleBtn.Parent = Row
    ToggleBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    ToggleBtn.Position = UDim2.new(0.55, 0, 0.2, 0)
    ToggleBtn.Size = UDim2.new(0, 50, 0, 26)
    ToggleBtn.Text = ""
    corner(ToggleBtn, 13)

    Circle.Parent = ToggleBtn
    Circle.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    Circle.Position = UDim2.new(0, 3, 0.5, -10)
    Circle.Size = UDim2.new(0, 20, 0, 20)
    corner(Circle, 10)

    if Toggles[configKey] then
        Circle.Position = UDim2.new(1, -23, 0.5, -10)
    end

    ToggleBtn.MouseButton1Click:Connect(function()
        Toggles[configKey] = not Toggles[configKey]
        local targetPos = Toggles[configKey] and UDim2.new(1, -23, 0.5, -10) or UDim2.new(0, 3, 0.5, -10)
        TweenService:Create(Circle, TweenInfo.new(0.2), {Position = targetPos}):Play()
        
        if configKey == "InstantSteal" then
            ShowSteal(Toggles[configKey])
        elseif configKey == "UnlockBase" then
            ShowUnlock(Toggles[configKey])
        elseif configKey == "StealSpeed" then
            if Toggles[configKey] then
                StartSpeed()
            else
                StopSpeed()
            end
        end
    end)

    if hasInput then
        local Input = Instance.new("TextBox")
        Input.Parent = Row
        Input.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        Input.Position = UDim2.new(0.78, 0, 0.15, 0)
        Input.Size = UDim2.new(0, 60, 0, 30)
        Input.Font = Enum.Font.GothamBold
        Input.Text = tostring(Values.SpeedAmount)
        Input.TextColor3 = Color3.fromRGB(255, 255, 255)
        Input.TextSize = 14
        corner(Input, 6)
        
        Input.FocusLost:Connect(function()
            local val = tonumber(Input.Text)
            if val then
                Values.SpeedAmount = math.clamp(val, 26, 36)
                Input.Text = tostring(Values.SpeedAmount)
            else
                Input.Text = tostring(Values.SpeedAmount)
            end
        end)
    end
end

-- =============================================
-- MENU PRINCIPAL
-- =============================================
local function CreateMenu()
    if ScreenGui then ScreenGui:Destroy() end
    
    ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "LawHub"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.Parent = PlayerGui
    
    MainFrame = Instance.new("Frame")
    MainFrame.Parent = ScreenGui
    MainFrame.BackgroundColor3 = Color3.fromRGB(45, 65, 80)
    MainFrame.BackgroundTransparency = 0.1
    MainFrame.Position = UDim2.new(0.5, -150, 0.5, -200)
    MainFrame.Size = UDim2.new(0, 320, 0, 420)
    corner(MainFrame, 15)
    
    local Title = Instance.new("TextLabel", MainFrame)
    Title.BackgroundTransparency = 1
    Title.Position = UDim2.new(0, 0, 0, 15)
    Title.Size = UDim2.new(1, 0, 0, 30)
    Title.Font = Enum.Font.GothamBold
    Title.Text = "LAW HUB"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.TextSize = 22
    
    local Subtitle = Instance.new("TextLabel", MainFrame)
    Subtitle.BackgroundTransparency = 1
    Subtitle.Position = UDim2.new(0, 0, 1, -35)
    Subtitle.Size = UDim2.new(1, 0, 0, 20)
    Subtitle.Font = Enum.Font.Gotham
    Subtitle.Text = "lawhub.gg"
    Subtitle.TextColor3 = Color3.fromRGB(200, 200, 200)
    Subtitle.TextSize = 14
    
    local Scroll = Instance.new("ScrollingFrame", MainFrame)
    Scroll.BackgroundTransparency = 1
    Scroll.Position = UDim2.new(0, 15, 0, 60)
    Scroll.Size = UDim2.new(1, -30, 1, -110)
    Scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    Scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
    Scroll.ScrollBarThickness = 0
    
    local Layout = Instance.new("UIListLayout", Scroll)
    Layout.SortOrder = Enum.SortOrder.LayoutOrder
    Layout.Padding = UDim.new(0, 12)
    
    CreateToggle(Scroll, "Instant Steal", "InstantSteal", false)
    CreateToggle(Scroll, "Auto Grab", "AutoGrab", false)
    CreateToggle(Scroll, "Active Potion", "ActivePotion", false)
    CreateToggle(Scroll, "Unlock Base", "UnlockBase", false)
    CreateToggle(Scroll, "Steal Speed", "StealSpeed", true)
    
    -- DRAG
    local dragToggle = false
    local dragStart = nil
    local startPos = nil
    
    MainFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragToggle = true
            dragStart = input.Position
            startPos = MainFrame.Position
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragToggle and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragToggle = false
        end
    end)
end

-- =============================================
-- KEYBIND
-- =============================================
UserInputService.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == currentKeybind then
        DoTeleport()
    end
end)

-- =============================================
-- INICIAR
-- =============================================
CreateMenu()

if Toggles.InstantSteal then ShowSteal(true) end
if Toggles.UnlockBase then ShowUnlock(true) end
if Toggles.StealSpeed then StartSpeed() end

print("=== LAW HUB CARREGADO ===")
print("Clique no toggle Instant Steal para abrir o painel")
print("Pressione F para teleportar")
